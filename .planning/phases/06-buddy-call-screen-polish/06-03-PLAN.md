---
phase: 06-buddy-call-screen-polish
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: []
autonomous: false
requirements: [BOARD-03, UX-01, UX-02]
user_setup: []

must_haves:
  truths:
    - "The buddy call screen count is dominant and readable at arm's length under bright conditions"
    - "Taking the network offline shows the Disconnected banner on the buddy call screen"
    - "The session board has no horizontal scroll and no broken layout at 375px, 768px, and 1024px"
    - "The AddPairForm submit button is full-width at 375px and content-width at 768px+"
    - "The 'View all pairs' link opens the buddy call screen and 'Back to board' returns to the session board"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the Phase 6 deliverables that cannot be proven in jsdom: responsive CSS breakpoints (no media-query support in jsdom) and the outdoor-readability / live-disconnect behavior of the buddy call screen. This plan runs after both Wave 1 plans (06-01 buddy call screen, 06-02 responsive polish) are merged. No code is written here — it gates the phase on a real-browser pass across iPhone, iPad, and laptop viewports.

Purpose: This is poolside safety UI. A count that is too small to read in sunlight, a layout that horizontally scrolls on an iPad, or a disconnect that shows a stale number as live would all defeat the buddy call. Automated tests cannot perceive font size, scroll overflow, or a live WebSocket drop — a human must.
Output: A verification pass (or a list of concrete defects to feed back into a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-buddy-call-screen-polish/06-VALIDATION.md
@.planning/phases/06-buddy-call-screen-polish/06-UI-SPEC.md
@.planning/ROADMAP.md

# All manual-only verifications are enumerated in 06-VALIDATION.md "Manual-Only Verifications".
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify buddy call screen + responsive board across devices</name>
  <action>
    This is a human verification checkpoint — no code changes. Start the dev server (`npm run dev`), then walk the verifier through the eight checks in <how-to-verify> and record the outcome. Pause for the human and do not mark the phase complete until they reply with the resume signal.
  </action>
  <what-built>
    Plan 06-01 added the buddy call screen at /pools/[poolId]/buddy-call (giant live swimmer/pair count, ConnectionBanner, "Back to board" link). Plan 06-02 made the SessionHeader responsive (two-row below md, single-row md+), added the "View all pairs" link, and made the AddPairForm submit button full-width on mobile. The dev server should be running (`npm run dev`).
  </what-built>
  <how-to-verify>
    Open an active session: navigate to /pools, pick a pool to reach /pools/[poolId], and add at least 2 pairs (include one trio) so the counts are non-zero.

    A. Buddy call screen (BOARD-03):
       1. Click "View all pairs" in the SessionHeader → it must navigate to /pools/[poolId]/buddy-call.
       2. Confirm the swimmer count and pair count are the dominant elements, very large (text-6xl), high-contrast slate-900 on white, readable at arm's length.
       3. If a pair list is shown, confirm names read "First Last / First Last" (trio shows all three joined by " / "), no camper codes.
       4. Open DevTools → Network → set Offline. Within a few seconds the amber "Disconnected — data may be stale." banner must appear and the last count must remain (not reset to 0). Set back Online → banner clears.
       5. Click "Back to board" → it must return to /pools/[poolId].

    B. Responsive session board (UX-01 / UX-02) — use DevTools device toolbar:
       6. 375px (iPhone): pool name + Close/Logout on the top row, the count centered on its own row below; the AddPairForm submit button spans full width; the CamperField inputs are stacked vertically. Pan horizontally — there must be NO horizontal scroll.
       7. 768px (iPad): pool name, count, and controls on a single row; CamperField inputs side-by-side; submit button sized to content. No horizontal scroll.
       8. 1024px (laptop): same single-row layout, no overflow, all controls reachable.
  </how-to-verify>
  <resume-signal>Type "approved" if all eight checks pass, or list each failing check with the viewport and observed behavior so a gap-closure plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan writes no code. Verifies the mitigation T-06-02 (ConnectionBanner shows disconnect, count never silently reset) from Plan 06-01 behaves correctly under a real WebSocket drop.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Spoofing | buddy call live count | mitigate (verify) | Human confirms the Disconnected banner appears on network offline and the stale count is not presented as live (check A.4) |
</threat_model>

<verification>
- All eight manual checks in Task 1 pass in a real browser across 375px / 768px / 1024px viewports plus the offline/online disconnect test.
</verification>

<success_criteria>
- BOARD-03: buddy call screen is readable outdoors, updates live, and honestly reports disconnect.
- UX-01: iPad and laptop layouts are single-row with no horizontal scroll and all controls reachable.
- UX-02: iPhone layout stacks the count and full-width submit button with nothing hidden or broken.
</success_criteria>

<output>
Create `.planning/phases/06-buddy-call-screen-polish/06-03-SUMMARY.md` recording the verification result (pass, or the list of defects) when done.
</output>
