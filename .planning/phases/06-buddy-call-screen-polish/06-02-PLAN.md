---
phase: 06-buddy-call-screen-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx
  - src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx
autonomous: true
requirements: [UX-01, UX-02, BOARD-03]
user_setup: []

must_haves:
  truths:
    - "On md+ the SessionHeader shows pool name, count, and controls on a single row"
    - "Below md the SessionHeader stacks the count on its own centered row beneath the pool-name/controls row"
    - "The SessionHeader has a 'View all pairs' link that navigates to /pools/[poolId]/buddy-call"
    - "On mobile the AddPairForm submit button spans the full width; on md+ it sizes to content"
  artifacts:
    - path: "src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx"
      provides: "Responsive two-row header (D-10) + View all pairs link"
      contains: "md:hidden"
    - path: "src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx"
      provides: "Full-width submit button on mobile (D-11)"
      contains: "w-full md:w-auto"
  key_links:
    - from: "src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx"
      to: "/pools/[poolId]/buddy-call"
      via: "next/link Link href"
      pattern: "buddy-call"
---

<objective>
Responsive polish on the session board so it is fully usable on iPad and laptop (UX-01) and best-effort usable on iPhone (UX-02), and wire the long-planned "View all pairs" link in the SessionHeader to the new buddy-call route (the access path for BOARD-03). Two existing client components change: `SessionHeader` gets a two-row mobile layout (D-10) plus the buddy-call link, and `AddPairForm` gets a full-width submit button on mobile (D-11). CamperField stacking is already implemented (Phase 5-02) — do not re-do it.

Purpose: The counselor's primary devices are iPad and laptop; iPhone is best-effort. The current SessionHeader packs pool name + 4xl count + controls into one flex row that overflows on narrow screens. The AddPairForm submit button currently sizes to content and looks stranded on a stacked mobile form.
Output: Updated SessionHeader.tsx and AddPairForm.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-buddy-call-screen-polish/06-CONTEXT.md
@.planning/phases/06-buddy-call-screen-polish/06-UI-SPEC.md
@.planning/phases/05-real-time-live-board/05-UI-SPEC.md

# Tailwind v4, md: breakpoint = 768px. No new packages. lucide-react already installed.

<interfaces>
SessionHeader current — src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx:
- "use client"; props { poolName, swimmerCount, pairCount, sessionId, poolId }
- single `<div className="flex items-center justify-between px-4 py-3">` with pool-name span, count div (text-4xl), and a controls div (CloseSessionDialog + LogoutButton).

AddPairForm current — src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx:
- CamperField container already `flex flex-col gap-4 md:flex md:flex-row md:gap-3 md:items-end` (line 68) — DO NOT change.
- submit button (line 121-128) className currently `min-h-[44px] px-6 bg-blue-600 ...` — missing a width class.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionHeader responsive two-row layout + View all pairs link</name>
  <files>src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx</files>
  <read_first>
    - src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx (current single-row layout)
    - .planning/phases/06-buddy-call-screen-polish/06-UI-SPEC.md (SessionHeader responsive section — exact `hidden md:block` / `md:hidden` pattern)
    - .planning/phases/06-buddy-call-screen-polish/06-CONTEXT.md (D-10 layout intent, Open Question 2 — link placement is Claude's discretion)
  </read_first>
  <action>
    Restructure the `<header>` per 06-UI-SPEC.md (D-10). Keep the existing sticky header classes `sticky top-0 z-10 bg-white border-b border-slate-200`. Inside:
    1. Top row `<div className="flex items-center justify-between px-4 py-3">` containing: the pool-name span (`text-xl font-semibold text-slate-900`); an inline count block visible only on md+ (`<div className="hidden md:block text-center">` wrapping the existing `<p className="text-4xl font-semibold text-slate-900 leading-none">{swimmerCount} swimmers · {pairCount} pairs</p>`); and the existing controls div (CloseSessionDialog + LogoutButton) unchanged.
    2. A second count row visible only below md: `<div className="md:hidden text-center px-4 pb-3">` wrapping the same `<p className="text-4xl ...">` count markup.
    3. Add a "View all pairs" link using `next/link`: `import Link from "next/link"` and render `<Link href={\`/pools/${poolId}/buddy-call\`}>` with a ChevronRight icon (`import { ChevronRight } from "lucide-react"`, size 16) and the text "View all pairs", styled `inline-flex items-center gap-1 text-base text-slate-600 hover:text-slate-900 min-h-[44px]`. Place it beneath the count: inside the `hidden md:block` count block on md+ and inside the `md:hidden` count row on mobile (so it appears below the count in both layouts). The link MUST resolve to `/pools/${poolId}/buddy-call` — poolId is already a prop.
    Do not change the LogoutButton or CloseSessionDialog props. Keep the count text format exactly `{swimmerCount} swimmers · {pairCount} pairs`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - SessionHeader.tsx contains `import Link from "next/link"` and a `<Link href={\`/pools/${poolId}/buddy-call\`}` usage
    - SessionHeader.tsx contains both `hidden md:block` (inline count on md+) and `md:hidden` (stacked count row below md)
    - SessionHeader.tsx contains the literal text `View all pairs` and imports `ChevronRight` from lucide-react
    - The count markup `{swimmerCount} swimmers · {pairCount} pairs` appears in both the md+ inline block and the md:hidden row
    - `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0
  </acceptance_criteria>
  <done>SessionHeader is single-row on md+ and two-row (count stacked, centered) below md, and exposes a working "View all pairs" link to the buddy-call route.</done>
</task>

<task type="auto">
  <name>Task 2: AddPairForm submit button full-width on mobile</name>
  <files>src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx</files>
  <read_first>
    - src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx (submit button at lines 121-128; CamperField container at line 68 is already responsive — do NOT touch it)
    - .planning/phases/06-buddy-call-screen-polish/06-UI-SPEC.md (AddPairForm section — only the submit button width changes)
  </read_first>
  <action>
    On the submit `<button type="submit">` (currently className `min-h-[44px] px-6 bg-blue-600 hover:bg-blue-700 text-white text-base font-semibold rounded-md disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-150`), insert `w-full md:w-auto` immediately after `min-h-[44px]` so the button spans full width on mobile and sizes to content on md+. Change nothing else: do not modify the CamperField container (line 68, already `flex-col ... md:flex-row`), the trio `+` button, or any handler logic. This is the only AddPairForm change for Phase 6 (Pitfall 4 — do not over-engineer additional breakpoints).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - AddPairForm.tsx submit button className contains `min-h-[44px] w-full md:w-auto`
    - AddPairForm.tsx still contains the unchanged CamperField container class `flex flex-col gap-4 md:flex md:flex-row md:gap-3 md:items-end`
    - `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0
  </acceptance_criteria>
  <done>The AddPairForm submit button is full-width on mobile and content-width on md+; no other form behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → SessionHeader Link | poolId interpolated into a same-origin internal route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | SessionHeader buddy-call Link href | accept | poolId originates from the already-validated route param rendered by the server page; the Link only navigates to a same-origin (protected) route that re-validates poolId server-side (Plan 06-01 T-06-01) |
| T-06-SC | Tampering | npm installs | accept | No new packages installed; ChevronRight is from already-installed lucide-react |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx vitest run` full suite green (CSS-only changes do not break existing tests).
- Manual (deferred to Plan 06-03 checkpoint): DevTools viewport at 375px / 768px / 1024px — verify count stacks below md and is inline on md+, submit button is full-width at 375px, and there is no horizontal scroll.
</verification>

<success_criteria>
- UX-01: On iPad/laptop (md+) the session board layout is single-row and uncluttered; no overflow.
- UX-02: On iPhone (375px) the count stacks below the pool name/controls and the submit button is full-width; nothing is hidden or broken.
- BOARD-03 (access): the "View all pairs" link opens /pools/[poolId]/buddy-call.
</success_criteria>

<output>
Create `.planning/phases/06-buddy-call-screen-polish/06-02-SUMMARY.md` when done.
</output>
