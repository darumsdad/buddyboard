---
phase: "02.1-user-name-fields"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - scripts/backfill-admin-name.ts
  - src/app/(admin)/admin/users/actions.ts
  - src/app/(admin)/admin/users/components/CreateUserModal.tsx
  - src/app/(admin)/admin/users/components/UserTable.tsx
  - src/app/(admin)/admin/users/page.tsx
autonomous: false
requirements:
  - AUTH-03

must_haves:
  truths:
    - "Admin can enter first name and last name when creating a new user account"
    - "The User Management table shows a Name column (first + last) as the leftmost column"
    - "The seed admin account has firstName='Camp' and lastName='Admin' after the backfill runs"
    - "Rows with no name data display a dash — in the Name column instead of blank or undefined"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "firstName and lastName nullable columns on the user table"
      contains: "first_name"
    - path: "scripts/backfill-admin-name.ts"
      provides: "One-time script to set seed admin name"
      contains: "backfill"
    - path: "src/app/(admin)/admin/users/actions.ts"
      provides: "createUserAction reads firstName and lastName from FormData"
      contains: "firstName"
    - path: "src/app/(admin)/admin/users/components/CreateUserModal.tsx"
      provides: "First Name and Last Name fields in the create form"
      contains: "first-name"
    - path: "src/app/(admin)/admin/users/components/UserTable.tsx"
      provides: "Name column as first column; User type extended"
      contains: "firstName"
    - path: "src/app/(admin)/admin/users/page.tsx"
      provides: "Type cast extended to include firstName and lastName"
      contains: "firstName"
  key_links:
    - from: "src/app/(admin)/admin/users/components/CreateUserModal.tsx"
      to: "src/app/(admin)/admin/users/actions.ts"
      via: "FormData with name='first-name' and name='last-name' passed to createUserAction"
      pattern: "name=\"first-name\""
    - from: "src/app/(admin)/admin/users/actions.ts"
      to: "auth.api.createUser"
      via: "data: { username, firstName, lastName } in request body"
      pattern: "firstName"
    - from: "src/app/(admin)/admin/users/page.tsx"
      to: "src/app/(admin)/admin/users/components/UserTable.tsx"
      via: "users prop — type cast includes firstName and lastName"
      pattern: "firstName.*string.*null"
---

<objective>
Add first name and last name to the user management system: store the fields in the DB,
collect them in the CreateUser form, display them as the first column in the user table,
and backfill the existing seed admin account.

Purpose: Admins can identify counselor accounts by real name rather than login handles.
Output: Extended schema pushed to Supabase, backfill script, updated server action, updated
        CreateUserModal, updated UserTable, updated page type cast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-user-name-fields/02.1-CONTEXT.md
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════
     TASK 1 — Schema: add firstName + lastName columns
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 1: Add firstName and lastName nullable columns to Drizzle schema</name>
  <files>src/db/schema.ts</files>

  <read_first>
    src/db/schema.ts — read in full before editing; understand the existing column
    definitions on the user table (id, name, email, username, role, etc.) so the
    two new columns are placed correctly and match the existing style.
  </read_first>

  <action>
    In src/db/schema.ts, add two nullable text columns to the `user` pgTable definition,
    immediately after the `username` column and before `displayUsername`:

      firstName: text("first_name"),
      lastName: text("last_name"),

    Rules:
    - Both are nullable (no `.notNull()`, no `.default()`). The DB allows null so
      existing rows survive the migration without a default value.
    - Use the exact camelCase keys `firstName` and `lastName` (TypeScript side) and
      the snake_case DB column names `"first_name"` and `"last_name"` (Postgres side).
    - Do NOT change the `name` field — it remains `text("name").notNull()` and continues
      to be set to `username` in the server action (per D-04, leave it unchanged).
    - No other file is touched in this task.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - src/db/schema.ts contains `firstName: text("first_name"),` (exact string, no notNull)
    - src/db/schema.ts contains `lastName: text("last_name"),` (exact string, no notNull)
    - The existing `name: text("name").notNull()` line is unchanged
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>schema.ts has two new nullable text columns; TypeScript compiles clean</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════
     TASK 2 — [BLOCKING] Push schema to Supabase
     ═══════════════════════════════════════════════════════════════ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push Drizzle schema to Supabase</name>

  <read_first>
    src/db/schema.ts — confirm Task 1 edits are present before proceeding.
    Review the two new column definitions (first_name, last_name) so you can
    confirm they appear in the drizzle-kit diff output.
  </read_first>

  <what-built>
    Task 1 added `firstName: text("first_name")` and `lastName: text("last_name")`
    to the Drizzle schema. This checkpoint pushes those column additions to the live
    Supabase database. The columns must exist in Postgres before the backfill script
    or any createUser call can write to them.
  </what-built>

  <how-to-verify>
    Run in your terminal:

      npx drizzle-kit push

    Drizzle will show a diff and ask you to confirm. Review the output:
    - Expect two ALTER TABLE statements adding `first_name text` and `last_name text`
      to the `user` table (both nullable, no default).
    - Confirm the prompt (type "yes" or press Enter as prompted).
    - Wait for "Changes applied" or equivalent success message.

    After confirming, verify in Supabase:
    - Open Supabase Dashboard → Table Editor → `user` table
    - Confirm columns `first_name` and `last_name` appear (both nullable, text type)
  </how-to-verify>

  <resume-signal>
    Type "pushed" when drizzle-kit reports success and you have confirmed the columns
    exist in Supabase. Describe any errors if the push failed.
  </resume-signal>

  <acceptance_criteria>
    - drizzle-kit push exits successfully (no error output)
    - Supabase `user` table has columns `first_name` (text, nullable) and `last_name`
      (text, nullable) visible in the dashboard
  </acceptance_criteria>

  <done>
    first_name and last_name columns exist in the live Supabase user table
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════
     TASK 3 — Backfill script for seed admin
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 3: Create and run backfill-admin-name.ts for the seed admin account</name>
  <files>scripts/backfill-admin-name.ts</files>

  <read_first>
    scripts/backfill-admin-role.ts — read in full; this is the canonical pattern to
    replicate exactly (dotenv config first, dynamic imports for db/schema/drizzle-orm,
    .update().set().where().returning() structure, exit codes, console output).
  </read_first>

  <action>
    Create scripts/backfill-admin-name.ts following the exact structure of
    scripts/backfill-admin-role.ts:

    - Static import: `import { config } from "dotenv";`
    - Call `config({ path: ".env.local", override: true });` before any async work
    - Dynamic imports inside the async function: db, schema, eq from drizzle-orm
    - Set `firstName: "Camp"` and `lastName: "Admin"` on the row where
      `schema.user.username` equals `"admin"` (the seed admin account)
    - Use `.returning()` and check for empty results (exit 1 + console.error if not found)
    - Log the returned row and call `process.exit(0)` on success

    After creating the file, run the script:
      npx tsx scripts/backfill-admin-name.ts

    The script must succeed (exit 0) before this task is considered done.
  </action>

  <verify>
    <automated>npx tsx scripts/backfill-admin-name.ts</automated>
  </verify>

  <acceptance_criteria>
    - scripts/backfill-admin-name.ts exists and follows the backfill-admin-role.ts
      structure (dotenv config, dynamic imports, drizzle update, returning check,
      process.exit codes)
    - The script sets `firstName: "Camp"` and `lastName: "Admin"` on the row where
      `username = "admin"`
    - Running `npx tsx scripts/backfill-admin-name.ts` exits 0 and logs the updated row
    - The logged row contains `first_name: 'Camp'` and `last_name: 'Admin'`
  </acceptance_criteria>

  <done>
    Seed admin row has firstName='Camp' and lastName='Admin' in Supabase; script exits 0
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════
     TASK 4 — Server action: read and forward firstName/lastName
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 4: Update createUserAction to read and forward firstName and lastName</name>
  <files>src/app/(admin)/admin/users/actions.ts</files>

  <read_first>
    src/app/(admin)/admin/users/actions.ts — read in full to understand the current
    FormData field reads (username, password, role) and the auth.api.createUser body
    shape (`data: { username }`). The new fields slot in alongside username.
  </read_first>

  <action>
    In `createUserAction`:

    1. After reading `role` from FormData, add two new reads:
         const firstName = formData.get("first-name") as string;
         const lastName  = formData.get("last-name")  as string;

       Note the hyphenated names (`"first-name"`, `"last-name"`) — they must match the
       HTML `name` attributes that will be added to CreateUserModal in Task 5.

    2. Extend the `data` object in the auth.api.createUser body:
         data: { username, firstName, lastName },

       The `name: username` field stays unchanged (Better Auth built-in — leave it).
       Do NOT rename or remove any existing field. Only add firstName and lastName
       into the data spread.

    No other changes to this file.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - actions.ts contains `formData.get("first-name")` (hyphenated, exact string)
    - actions.ts contains `formData.get("last-name")` (hyphenated, exact string)
    - actions.ts contains `data: { username, firstName, lastName }` in the
      auth.api.createUser body
    - The `name: username` line is still present and unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    createUserAction reads firstName/lastName from FormData and passes them to
    auth.api.createUser via the data object; TypeScript compiles clean
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════
     TASK 5 — UI: CreateUserModal, UserTable, page.tsx
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task 5: Add name fields to CreateUserModal, Name column to UserTable, extend page.tsx cast</name>
  <files>
    src/app/(admin)/admin/users/components/CreateUserModal.tsx,
    src/app/(admin)/admin/users/components/UserTable.tsx,
    src/app/(admin)/admin/users/page.tsx
  </files>

  <read_first>
    src/app/(admin)/admin/users/components/CreateUserModal.tsx — read in full; note
    the exact Tailwind classes on the Username field (the new fields copy them exactly).
    src/app/(admin)/admin/users/components/UserTable.tsx — read in full; note the User
    type, thead th elements, tbody td elements, and colSpan on the empty-state row.
    src/app/(admin)/admin/users/page.tsx — read in full; note the `as any as Array<{...}>`
    cast that will be extended.
  </read_first>

  <action>
    ### CreateUserModal.tsx

    Insert two new `&lt;div&gt;` field blocks BEFORE the existing Username field (so field
    order becomes: First Name → Last Name → Username → Password → Role — per D-03).

    First Name field:
    ```
    id="first-name", name="first-name", type="text", required, autoComplete="given-name"
    placeholder="Enter first name"
    label text: "First Name"
    ```

    Last Name field:
    ```
    id="last-name", name="last-name", type="text", required, autoComplete="family-name"
    placeholder="Enter last name"
    label text: "Last Name"
    ```

    Both inputs use EXACTLY the same Tailwind classes as the existing Username input:
      className="min-h-[44px] w-full border border-slate-300 rounded-md px-3 text-base
                 text-slate-900 placeholder:text-slate-500 focus:border-blue-500
                 focus:ring-1 focus:ring-blue-500 focus:outline-none"

    Both labels use EXACTLY the same classes as the Username label:
      className="text-base font-semibold text-slate-900"

    ### UserTable.tsx

    1. Extend the `User` type with two new optional fields:
         firstName: string | null;
         lastName:  string | null;

    2. Add a "Name" `&lt;th&gt;` as the FIRST header cell (before Username), using the same
       classes as existing th elements:
         className="text-sm font-semibold text-slate-900 px-4 py-3 text-left"

    3. Add a "Name" `&lt;td&gt;` as the FIRST data cell in each `users.map()` row:
         className="text-base text-slate-900 px-4 py-3"
       Content: `{user.firstName && user.lastName ? \`${user.firstName} ${user.lastName}\` : "—"}`
       The fallback character is an em dash — not a hyphen, not an empty string.

    4. Update the empty-state row's `colSpan` from `4` to `5` (one extra column).

    ### page.tsx

    Extend the `as any as Array<{...}>` cast to include the two new fields:
      firstName: string | null;
      lastName:  string | null;

    Add these two lines alongside the existing `username`, `name`, `role`, `createdAt`
    fields inside the type literal. Do not change any other part of this file.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    CreateUserModal.tsx:
    - Contains `name="first-name"` input with `autoComplete="given-name"` and `required`
    - Contains `name="last-name"` input with `autoComplete="family-name"` and `required`
    - First Name field appears before Last Name, which appears before the Username field
      (verified by line order in the file)
    - New inputs use `min-h-[44px]` in their className (same as Username input)

    UserTable.tsx:
    - `User` type contains `firstName: string | null` and `lastName: string | null`
    - `&lt;thead&gt;` first `&lt;th&gt;` is "Name" (before "Username")
    - `&lt;tbody&gt;` first `&lt;td&gt;` renders the name or em dash fallback
    - The em dash fallback uses the literal character `—` (U+2014), not a hyphen
    - Empty-state `colSpan` is `5`

    page.tsx:
    - The `as any as Array<{...}>` type literal contains `firstName: string | null`
    - The `as any as Array<{...}>` type literal contains `lastName: string | null`

    All files:
    - `npx tsc --noEmit` exits 0 with no type errors
  </acceptance_criteria>

  <done>
    CreateUserModal collects first/last name; UserTable shows Name as leftmost column
    with em dash fallback; page.tsx type cast includes firstName and lastName;
    TypeScript compiles clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → server action | firstName/lastName arrive via FormData from admin-only UI |
| server action → Better Auth API | data fields forwarded to auth.api.createUser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-01 | Spoofing | createUserAction | accept | requireAdmin() guard already enforces admin session; no change needed |
| T-2.1-02 | Tampering | firstName/lastName FormData fields | accept | Admin-only route; no PII risk beyond what already exists; no injection vector in text columns stored as-is |
| T-2.1-03 | Information Disclosure | UserTable — name displayed | accept | Name is visible only to admin role; same protection as username/role columns already in scope |
| T-2.1-SC | Tampering | npm/drizzle-kit install | accept | drizzle-kit already in devDependencies; no new package installs in this phase |
</threat_model>

<verification>
After all tasks complete, verify end-to-end:

1. Run `npx tsc --noEmit` — exits 0 (no TypeScript errors across all modified files)
2. Start dev server (`npm run dev`) and navigate to /admin/users
3. Click "Create user" — confirm field order is: First Name → Last Name → Username →
   Password → Role
4. Create a test user with a real name — confirm the UserTable shows Name as the first
   column with the entered name
5. Confirm the seed admin row shows "Camp Admin" in the Name column (backfill result)
6. Create a test and immediately delete it to verify the delete flow still works with the
   extra column present
</verification>

<success_criteria>
- Admin fills in First Name + Last Name when creating a new user (both required)
- Submitted first/last name is stored in the `first_name` / `last_name` columns in Supabase
- User Management table shows Name as leftmost column for all users
- Rows without a name (pre-backfill legacy data) show em dash — not blank/undefined
- Seed admin row shows "Camp Admin" after backfill
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/02.1-user-name-fields/02.1-01-SUMMARY.md` when done
</output>
