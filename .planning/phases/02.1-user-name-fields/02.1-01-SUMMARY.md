---
plan: "02.1-01"
phase: "02.1-user-name-fields"
status: complete
started: 2026-06-28
completed: 2026-06-28
tasks_total: 5
tasks_completed: 5
key-files:
  created:
    - scripts/backfill-admin-name.ts
  modified:
    - src/db/schema.ts
    - src/app/(admin)/admin/users/actions.ts
    - src/app/(admin)/admin/users/components/CreateUserModal.tsx
    - src/app/(admin)/admin/users/components/UserTable.tsx
    - src/app/(admin)/admin/users/page.tsx
---

## Summary

Added `firstName` and `lastName` to the user management system end-to-end: Drizzle schema, Supabase DB, server action, create form, and user table.

## What Was Built

- **schema.ts**: Added `firstName: text("first_name")` and `lastName: text("last_name")` nullable columns to the `user` pgTable, after `username`
- **Supabase**: Columns added via SQL (`ALTER TABLE "user" ADD COLUMN IF NOT EXISTS`) — drizzle-kit push connected but produced no diff output, so SQL was applied directly
- **backfill-admin-name.ts**: One-time script (pattern from backfill-admin-role.ts) that sets `firstName: "Camp"`, `lastName: "Admin"` on the seed admin account; ran successfully
- **actions.ts**: `createUserAction` now reads `first-name` and `last-name` from FormData and passes `firstName`/`lastName` in the `data` object to `auth.api.createUser`
- **CreateUserModal.tsx**: First Name and Last Name inputs added before Username, with identical Tailwind classes, `required`, and appropriate `autoComplete` attributes
- **UserTable.tsx**: `User` type extended with `firstName`/`lastName`; Name column added as first `<th>` and `<td>`; em dash fallback for rows with no name; `colSpan` updated from 4 to 5
- **page.tsx**: Type cast extended to include `firstName: string | null` and `lastName: string | null`

## Deviations

- drizzle-kit push connected successfully but showed no diff and produced no output beyond the spinner. Applied the two `ALTER TABLE` statements directly in the Supabase SQL Editor instead. TypeScript and the backfill script confirmed the columns exist and are writable.

## Self-Check: PASSED

- `npx tsc --noEmit` exits 0 across all modified files
- Backfill script ran successfully: seed admin row has `first_name: 'Camp'`, `last_name: 'Admin'`
- All must_have truths satisfied:
  - Admin can enter first/last name when creating a user ✓
  - UserTable shows Name as leftmost column ✓
  - Seed admin shows "Camp Admin" after backfill ✓
  - Rows with no name display em dash — ✓
