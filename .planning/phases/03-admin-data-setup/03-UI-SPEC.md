---
phase: 3
slug: admin-data-setup
status: updated-post-execution
shadcn_initialized: false
preset: none
created: 2026-06-28
updated: 2026-06-28
---

> **Post-execution update (2026-06-28):** Several UI patterns changed during manual testing and review. Sections marked ⚠️ reflect the actual shipped implementation, which differs from the original spec.

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Admin Data Setup.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

All values pre-populated from existing codebase patterns (login page, admin users page,
CreateUserModal, DeleteConfirmDialog, UserTable) and upstream decisions in CONTEXT.md and
RESEARCH.md. No user questions were required.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (raw Tailwind CSS) |
| Icon library | lucide-react (already in package.json) |
| Font | Geist Sans (CSS variable --font-geist-sans, set in globals.css) |

Source: `components.json` not found. `src/app/globals.css` confirms `--font-sans: var(--font-geist-sans)`.
Tailwind v4 via `@import "tailwindcss"` with no custom config file — all tokens are Tailwind defaults
overridden inline via class names throughout the codebase.

---

## Spacing Scale

Declared values (multiples of 4). Extracted from recurring class names across login, users page, and
admin components.

| Token | Value | Tailwind Class | Usage |
|-------|-------|----------------|-------|
| xs | 4px | gap-1 | Sidebar nav link gap |
| sm | 8px | gap-2, mt-2 | Button-row gaps, helper text separation |
| md | 16px | p-4, gap-4, px-4 | Form field gaps, table cell padding, nav padding |
| lg | 24px | p-6, mt-6 | Page container padding, dialog button area top margin |
| xl | 32px | p-8, mt-8 | Modal content padding, section top margin |
| 2xl | 48px | — | Not currently used; available if page-level breaks needed |
| 3xl | 64px | — | Not currently used |

Exceptions:
- sm+: 12px (`px-3 / py-3`) — exception from standard set; retained because `px-3 py-3` is the established input-field and table-row padding pattern extracted from Phase 1–2 components (CreateUserModal, DeleteConfirmDialog, UserTable); changing to 8px would collapse internal padding noticeably; this exception is pre-approved.
- Touch targets: `min-h-[44px]` on all interactive elements (inputs, buttons, table action links).
  This is a hard constraint across all phases — do not reduce below 44px.
- Modal max-width: `max-w-sm` (384px) — consistent with existing CreateUserModal and DeleteConfirmDialog.

---

## Typography

Exactly 4 sizes, 2 weights. Extracted from existing components and matched to Tailwind defaults.

| Role | Size | Tailwind Class | Weight | Weight Class | Line Height | Usage |
|------|------|----------------|--------|--------------|-------------|-------|
| Display | 30px | text-3xl | 600 | font-semibold | 1.2 | Page h1 headings ("Campers", "Pools") |
| Heading | 20px | text-xl | 600 | font-semibold | 1.2 | Dialog h2 headings ("Add camper", "Remove Pool?") |
| Label / Table header | 14px | text-sm | 600 | font-semibold | 1.4 | Table column headers, badge-style indicators |
| Body | 16px | text-base | 400 | (default) | 1.5 | All other text: table cells, form inputs, error messages, placeholder text |

Body labels (form field labels) use `text-base font-semibold` — body size at semibold weight.
This is the established pattern from CreateUserModal (`text-base font-semibold text-slate-900`).

Weights in use: 400 (body default) and 600 (font-semibold on headings, labels, active nav, inactive nav, buttons). No other weights are used.

---

## Color

| Role | Tailwind Token | Hex | Usage |
|------|----------------|-----|-------|
| Dominant (60%) | white | #ffffff | Page backgrounds (`bg-white`), table body, modal overlay content |
| Secondary (30%) | slate-50 | #f8fafc | Admin sidebar (`bg-slate-50`), table headers (`bg-slate-50`), some modal backgrounds, card surfaces |
| Accent (10%) | blue-600 | #2563eb | Reserved for specific elements listed below |
| Destructive | red-600 | #dc2626 | Reserved for specific elements listed below |

**Accent (blue-600) reserved for:**
- Primary action buttons: "Add camper", "Upload roster", "Add pool", "Save" in modals
- Active sidebar nav link: `bg-blue-600 text-white` on the current route
- Input focus ring: `focus:border-blue-500 focus:ring-1 focus:ring-blue-500`
- Hover state on inactive sidebar links: NOT used — inactive links use `hover:bg-slate-200`

**Destructive (red-600) reserved for:**
- Destructive confirmation buttons: "Remove camper", "Clear all campers" inside confirmation dialogs
- Destructive text triggers: "Delete user"-style text links with `text-red-600 hover:underline`
- Error message text: `text-red-600` inline error paragraphs
- Error list items in the import modal after a failed import

**Additional semantic colors (Tailwind defaults, not new tokens):**
- slate-900 (`#0f172a`): all primary text
- slate-700 (`#334155`): secondary text (dialog body copy, cancel buttons)
- slate-600 (`#475569`): tertiary text (modal close links)
- slate-500 (`#64748b`): placeholder text in inputs
- slate-300 (`#cbd5e1`): input borders (default state)
- slate-200 (`#e2e8f0`): table dividers, sidebar border, card borders
- green-700 (`#15803d`): import success message ("Imported N campers successfully.")

Source: All values read directly from class names in `login/page.tsx`, `CreateUserModal.tsx`,
`DeleteConfirmDialog.tsx`, `UserTable.tsx`.

---

## Layout Contract

### Admin Shell (applies to all admin pages in Phase 3) ⚠️

The `(admin)/layout.tsx` wraps children in a flex row with the sidebar and a `<Toaster>` (sonner).

```
┌──────────────────────────────────────────────────────────┐
│ AdminSidebar (w-48, shrink-0)  │  Main content (flex-1) │
│ bg-slate-50                    │  bg-white               │
│ border-r border-slate-200      │                         │
│ min-h-screen p-4               │                         │
│                                │                         │
│ [Users]                        │  <page content>         │
│ [Campers]  ← active            │                         │
│ [Pools]                        │                         │
│                                │                         │
│ ── divider ──                  │                         │
│ [↗ View Buddy Board]           │                         │
└──────────────────────────────────────────────────────────┘
```

"View Buddy Board" sits below a `border-t` divider at the bottom of the sidebar. Uses `ExternalLink` icon (14px), smaller text (`text-sm`), slate-500 colour. Links to `/pools`.

- Sidebar: `w-48 shrink-0 bg-slate-50 border-r border-slate-200 min-h-screen p-4`
- Active link: `block px-3 py-2 rounded-md text-base font-semibold bg-blue-600 text-white transition-colors`
- Inactive link: `block px-3 py-2 rounded-md text-base font-semibold text-slate-700 hover:bg-slate-200 transition-colors`
- Main content wrapper: `flex-1 min-w-0`
- Page container (inside main): `max-w-4xl mx-auto p-6`

Active and inactive sidebar links both use `font-semibold` (600). They are differentiated only by
background color: active uses `bg-blue-600 text-white`, inactive uses no background with
`hover:bg-slate-200`.

Source: RESEARCH.md Pattern 4 (AdminSidebar).

### Campers Page Layout ⚠️

```
Page heading row:   [h1 "Campers"]  ·  [Eraser icon]  [Download icon]  [Upload icon ← larger, separated]  [UserPlus icon]
Search row:         [🔍 Search input — full width]
Table:              [CamperTable — see below]
Pagination:         [← Previous]  Page X of Y  [Next →]
```

All header actions are **icon-only buttons** (lucide-react). Text labels were removed in favour of `title` / `aria-label` tooltip attributes.

- **Eraser** (`Eraser` icon, 20px) — Clear all campers. Slate colour, turns red on hover.
- **Download** (`Download` icon, 20px) — Downloads `public/sample-roster.csv` (CSV template). Direct `<a download>` link.
- **Upload** (`Upload` icon, 24px) — Import roster modal trigger. **Separated right** with `gap-4` from the other icons; rendered larger (24px vs 20px) to signal it as the primary action.
- **UserPlus** (`UserPlus` icon, 20px) — Add camper modal trigger.

SearchBar is now **full-width** with an inline `Search` icon on the left (`pl-9` offset). The import button no longer shares the search row.

### Camper Table Columns

| Column | Width | Notes |
|--------|-------|-------|
| First Name | auto | |
| Last Name | auto | |
| Code | auto | Displayed as-is (string — leading zeros preserved) |
| Bunk | auto | Empty cell shows "—" |
| Notes | auto | Empty cell shows "—"; truncate at 48 characters with ellipsis |
| Actions | shrink | "Edit" (slate link) + "Remove" (red link), same pattern as UserTable |

Table container: `bg-white border border-slate-200 rounded-md overflow-hidden mt-8` (matches UserTable)
Table header row: `bg-slate-50`, cells `text-sm font-semibold text-slate-900 px-4 py-3 text-left`
Table body rows: `divide-y divide-slate-200`, cells `text-base text-slate-900 px-4 py-3`

### Pools Page Layout ⚠️

```
Page heading row:   [h1 "Pools"]  ·  [Plus icon]
Pool list:          [Pool Name]  [Pencil icon]  [Trash2 icon]   (one row per pool)
```

- **Plus** (`Plus` icon, 20px) in the page header — opens "Add pool" modal. **No inline form below the list.**
- **Pencil** (`Pencil` icon, 16px) per row — opens Rename modal (same max-w-sm pattern).
- **Trash2** (`Trash2` icon, 16px) per row — opens remove confirmation dialog. Slate colour, turns red on hover.
- Add pool is now a **modal dialog** (same pattern as AddCamperModal) — single name field, submit closes on success. Inline form was removed for consistency.

---

## Interaction Contract

### Search (Campers)
- Input type: `type="search"` with `placeholder="Search by name or code..."`
- Debounce: 300ms (from CONTEXT.md D-10)
- Triggers: `router.push()` updating `?q=` and resetting `page=1`
- Server re-renders with filtered results
- No explicit submit button — search fires on typing after debounce

### Pagination
- Style: Prev/Next buttons flanking "Page X of Y" text
- Page size: 50 per page (from CONTEXT.md D-10)
- Prev/Next are standard `<button>` elements, disabled when on first/last page
- Disabled state: `disabled:opacity-50 disabled:cursor-not-allowed`
- Page indicator: `text-base text-slate-700`

### Modals (Add Camper, Edit Camper, Edit Pool, Import Roster)
- Backdrop: `fixed inset-0 bg-black/50 flex items-center justify-center z-50`
- Modal panel: `bg-white rounded-lg p-6 w-full max-w-sm shadow-sm` (or `bg-slate-50` for form modals)
  - AddCamperModal and EditCamperModal use `bg-slate-50` (matches CreateUserModal)
  - ImportModal uses `bg-white` (data display, not a form-only modal)
- Close: always present as a text link at the bottom: `text-base text-slate-600 hover:text-slate-900`
- Primary submit: `min-h-[44px] w-full bg-blue-600 hover:bg-blue-700 text-white text-base font-semibold rounded-md transition-colors duration-150 disabled:opacity-50 disabled:cursor-not-allowed`
- Loading state: button text changes to "Saving..." or "Importing..." with `disabled` applied

### Confirmation Dialogs (Remove Camper, Clear All Campers, Remove Pool)
- Same structure as existing DeleteConfirmDialog
- Two buttons side by side (flex gap-3): cancel (border/slate) + confirm (red/filled)
- Cancel label pattern: "Keep camper" / "Keep pool" (positive framing for cancel)
- Confirm label pattern: "Remove camper" / "Clear all campers" / "Remove pool"

### Import Modal States ⚠️

File format: **CSV** (not xlsx). Parsed server-side with `papaparse`. Same CampMinder column mapping.

1. **Default:** Merge / Replace radio buttons (labels only, no descriptions). Custom "Choose file…" button (hidden `<input type="file">` + styled `<button>` with `Paperclip` icon; shows selected filename). No template download link in dialog (moved to page header as `Download` icon).
2. **Pending:** "Uploading…" button text, disabled.
3. **Error:** Bordered `bg-red-50` box with count summary ("N issues found") + × dismiss button + scrollable list (`max-h-40 overflow-y-auto`). Modal capped at `max-h-[90vh]`.
4. **Success:** Modal auto-closes; sonner toast fires bottom-right: "N campers imported". No in-dialog success state shown.

**useActionState lifecycle fix:** `useActionState` is inside an inner `ImportForm` component that only mounts when the modal is open. This guarantees state resets to `null` on every open — no stale errors or success from prior sessions leak through.

**New dependencies:** `sonner@2.0.7` (toasts, `<Toaster>` in admin layout), `papaparse@5.5.4` (CSV parsing).

### Form Validation (Add/Edit Camper, Add Pool)
- Required fields: validated server-side via Server Action. No client-side custom validation beyond `required` attribute.
- Error display: single `<p role="alert" className="text-base text-red-600">` paragraph below the last field, above the submit button.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — campers | "Add camper" |
| Primary CTA — import | "Upload roster" |
| Primary CTA — pools | "Add pool" |
| Primary CTA — save (modals) | "Save camper" / "Save pool" |
| Camper table empty (no data) | Heading: "No campers yet" Body: "Upload a spreadsheet or add campers one at a time." |
| Camper table empty (search no match) | Heading: "No results for '[query]'" Body: "Try a different name or code." |
| Pool list empty | Heading: "No pools yet" Body: "Add a pool to get started. The system needs at least one pool before counselors can run sessions." |
| Import success | "Imported [N] camper[s] successfully." |
| Import error (header) | "Import failed — fix the following issues and try again:" |
| Import error (row example) | "Row 12: code is blank" / "Row 7: duplicate code '042' (also in row 23)" |
| Import file type error | "Please upload an Excel file (.xlsx or .xls)." |
| No file selected | "No file selected." |
| Remove camper — dialog heading | "Remove [First Last]?" |
| Remove camper — dialog body | "They will be permanently removed from the roster. This cannot be undone." |
| Remove camper — cancel | "Keep camper" |
| Remove camper — confirm | "Remove camper" |
| Clear all campers — trigger label | "Clear all campers" |
| Clear all campers — dialog heading | "Clear all campers?" |
| Clear all campers — dialog body | "This will permanently remove all campers from the roster. This cannot be undone." |
| Clear all campers — cancel | "Keep roster" |
| Clear all campers — confirm | "Clear all campers" |
| Remove pool — dialog heading | "Remove [Pool Name]?" |
| Remove pool — dialog body | "This pool will be permanently removed. Counselors will no longer be able to start sessions in this pool." |
| Remove pool — cancel | "Keep pool" |
| Remove pool — confirm | "Remove pool" |
| Generic action error | "Something went wrong. Please try again." |
| Rename pool — modal heading | "Rename pool" |
| Edit camper — modal heading | "Edit camper" |
| Add camper — modal heading | "Add camper" |
| Import roster — modal heading | "Import roster" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not required |
| third-party | none declared | not required |

No registries involved. This phase uses raw Tailwind CSS with hand-authored components
following the patterns established in Phases 1 and 2.

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md | 10 locked decisions (D-01 through D-10); all Claude's Discretion items resolved from codebase patterns |
| RESEARCH.md | Sidebar pattern (Pattern 4), SearchBar pattern (Pattern 2), import modal useActionState pattern |
| `src/app/(auth)/login/page.tsx` | Input classes, button classes, error paragraph, color tokens |
| `src/app/(admin)/admin/users/page.tsx` | Page heading, max-w-4xl container, flex header row |
| `src/app/(admin)/admin/users/components/CreateUserModal.tsx` | Modal structure, form layout, label classes, loading state copy pattern |
| `src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` | Confirm dialog layout, button pairing, error paragraph |
| `src/app/(admin)/admin/users/components/UserTable.tsx` | Table markup, column header classes, cell classes, empty state |
| `src/app/globals.css` | Font (Geist Sans), base color variables |
| User input | None required |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
