---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - drizzle.config.ts
  - .env.example
  - .prettierrc
  - vitest.config.ts
  - src/db/index.ts
  - src/db/schema.ts
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/app/api/auth/[...all]/route.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "npm run build exits with code 0 (project compiles)"
    - "npx tsc --noEmit exits with code 0 (zero TypeScript errors)"
    - "All Better Auth, Drizzle, and postgres packages are installed"
    - "src/db/schema.ts exists with exported user, session, account, and verification tables (generated by Better Auth CLI)"
    - "drizzle/ directory contains at least one .sql migration file (generated by drizzle-kit)"
    - "BETTER_AUTH_SECRET and DATABASE_URL_DIRECT are documented in .env.example (not committed with real values)"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "Better Auth server instance with username plugin, 5h session, Drizzle adapter"
      exports: ["auth", "Session"]
    - path: "src/lib/auth-client.ts"
      provides: "Browser auth client with username plugin"
      exports: ["authClient"]
    - path: "src/app/api/auth/[...all]/route.ts"
      provides: "Better Auth HTTP handler for all /api/auth/* endpoints"
      exports: ["GET", "POST"]
    - path: "src/db/index.ts"
      provides: "Drizzle db instance connected to Supabase Transaction pooler"
      exports: ["db"]
    - path: "src/db/schema.ts"
      provides: "Better Auth tables (user, session, account, verification)"
      contains: "export const user"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit config using DATABASE_URL_DIRECT for migrations"
      contains: "DATABASE_URL_DIRECT"
    - path: ".env.example"
      provides: "Documentation of all required environment variables"
      contains: "DATABASE_URL="
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/db/index.ts"
      via: "drizzleAdapter(db, { provider: 'pg', schema: {...} })"
      pattern: "drizzleAdapter\\(db"
    - from: "src/app/api/auth/[...all]/route.ts"
      to: "src/lib/auth.ts"
      via: "toNextJsHandler(auth)"
      pattern: "toNextJsHandler\\(auth\\)"
    - from: "src/lib/auth-client.ts"
      to: "/api/auth/*"
      via: "createAuthClient({ baseURL: process.env.NEXT_PUBLIC_APP_URL })"
      pattern: "NEXT_PUBLIC_APP_URL"
---

## Phase Goal

**As a** counselor, **I want to** navigate to the app URL and log in with my username and password, **so that** I can access the pool management screens with a session that persists across browser restarts.

> Note: ROADMAP.md Phase 1 goal ("Developer can deploy the app and counselors can log in securely") is not in user-story format. This story derives from the four ROADMAP success criteria. Run `/gsd mvp-phase 1` to update the ROADMAP goal to user-story format.

<objective>
This plan bootstraps the entire BuddyBoard project from an empty repository to a fully-wired Next.js application with Better Auth, Drizzle ORM, and all schema generation complete. At the end of this plan, every file needed to authenticate users exists and type-checks — the only missing piece is a live Supabase database (handled in 01-PLAN-db).

Purpose: Establish the foundation every subsequent phase builds on. The architectural decisions made here (directory structure, auth wiring pattern, session config, migration approach) are locked for the life of the project.

Output: Working Next.js 16 project with all auth packages installed, Better Auth fully configured for username/password authentication with 5-hour idle sessions, Drizzle schema generated and SQL migration file ready to apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Sourced from RESEARCH.md. -->
<!-- Use these directly — no exploration needed. -->

src/db/index.ts must use:
  import { drizzle } from "drizzle-orm/postgres-js"
  import postgres from "postgres"
  const client = postgres(process.env.DATABASE_URL!, { prepare: false })
  export const db = drizzle(client, { schema })
  Note: prepare:false is REQUIRED for Supabase Transaction pooler (port 6543)

src/lib/auth.ts must use:
  betterAuth({ baseURL, secret, database: drizzleAdapter(db, { provider: "pg", schema: {...} }), emailAndPassword: { enabled: true }, plugins: [username()], session: { expiresIn: 60*60*5, updateAge: 60*30 } })
  Session config: expiresIn=18000 (5h idle window per D-05), updateAge=1800 (30min refresh per D-05/Pitfall 7)

src/lib/auth-client.ts must use:
  createAuthClient({ baseURL: process.env.NEXT_PUBLIC_APP_URL, plugins: [usernameClient()] })
  Note: usernameClient() is REQUIRED — without it, authClient.signIn.username is not a function

src/app/api/auth/[...all]/route.ts must use:
  toNextJsHandler(auth)  -- NOT toNodeHandler (that is Pages Router only)

drizzle.config.ts must use:
  dbCredentials: { url: process.env.DATABASE_URL_DIRECT! }
  Note: DATABASE_URL_DIRECT is the Supabase direct connection (port 5432), required for migrations
  DATABASE_URL (port 6543 Transaction pooler) is for the app runtime only
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap project scaffold and developer tooling</name>
  <files>package.json, tsconfig.json, next.config.ts, drizzle.config.ts, .env.example, .prettierrc, vitest.config.ts, .gitignore</files>
  <read_first>
    Check whether package.json already exists before running create-next-app: if it does, skip the scaffold step and proceed directly to package installs and config files. After create-next-app runs, read package.json (to see what scripts and dependencies were generated) and tsconfig.json (to verify strict mode and path aliases) before making any modifications.
  </read_first>
  <action>
    Run `npx create-next-app@16.2.9 . --typescript --tailwind --eslint --app --src-dir --no-import-alias` to initialize the project in the current directory. When prompted for project name, press Enter to accept the directory name. When asked about Turbopack, accept the default (yes). When asked about src/ directory, yes. When asked about App Router, yes.

    After scaffold completes, install additional production dependencies: `npm install better-auth @better-auth/drizzle-adapter drizzle-orm postgres`

    Install additional dev dependencies: `npm install -D drizzle-kit @better-auth/cli vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @types/node`

    Add test scripts to package.json scripts block: `"test": "vitest run"` and `"test:watch": "vitest"`. Keep all existing scripts from create-next-app.

    Verify tsconfig.json has `"strict": true` inside compilerOptions — if not, add it. Verify `"paths": { "@/*": ["./src/*"] }` exists in compilerOptions — create-next-app with --no-import-alias sets this; confirm it is present.

    Create `.prettierrc` in the project root with: `semi` true, `singleQuote` false, `tabWidth` 2, `printWidth` 80, `trailingComma` "es5". Plain JSON format, no comments.

    Create `drizzle.config.ts` in the project root. Import `defineConfig` from `drizzle-kit` and `dotenv/config` (add the dotenv import at the top so DATABASE_URL_DIRECT loads from .env.local). Configure: `out: "./drizzle"`, `schema: "./src/db/schema.ts"`, `dialect: "postgresql"`, `dbCredentials: { url: process.env.DATABASE_URL_DIRECT! }`. The exclamation mark is intentional — DATABASE_URL_DIRECT is required for migrations and the build will fail clearly if it is missing.

    Create `vitest.config.ts` in the project root. Import `defineConfig` from `vitest/config` and `react` plugin from `@vitejs/plugin-react`. Configure: plugins with the react plugin, test environment `jsdom`, globals `true`, include pattern `["src/**/*.test.{ts,tsx}"]`, and setupFiles pointing to a setup file at `src/test/setup.ts`. Create `src/test/setup.ts` with one line: `import "@testing-library/jest-dom"`.

    Create `.env.example` in the project root documenting every required variable with empty values and comments explaining each:
    - DATABASE_URL= (Supabase Transaction pooler URL, port 6543 — used by the app at runtime)
    - DATABASE_URL_DIRECT= (Supabase direct connection URL, port 5432 — used by drizzle-kit migrate only)
    - BETTER_AUTH_SECRET= (32+ character random string — generate with: openssl rand -base64 32)
    - BETTER_AUTH_URL= (Production app URL, e.g. https://buddyboard.vercel.app — used by Better Auth for redirect URLs)
    - NEXT_PUBLIC_APP_URL= (Same as BETTER_AUTH_URL — exposed to the browser for auth client baseURL)

    Verify `.gitignore` contains `.env.local` — create-next-app should have added it, but confirm. Also verify `.env.local` is NOT already committed to git.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"next"` with version `"16.2.9"` or compatible range in dependencies
    - package.json contains `"better-auth"`, `"@better-auth/drizzle-adapter"`, `"drizzle-orm"`, `"postgres"` in dependencies
    - package.json contains `"drizzle-kit"`, `"@better-auth/cli"`, `"vitest"`, `"@testing-library/react"` in devDependencies
    - package.json scripts block contains `"test": "vitest run"`
    - tsconfig.json compilerOptions contains `"strict": true`
    - tsconfig.json compilerOptions paths contains `"@/*": ["./src/*"]`
    - drizzle.config.ts contains `DATABASE_URL_DIRECT`
    - drizzle.config.ts contains `out: "./drizzle"`
    - drizzle.config.ts contains `schema: "./src/db/schema.ts"`
    - .env.example contains `DATABASE_URL=`
    - .env.example contains `DATABASE_URL_DIRECT=`
    - .env.example contains `BETTER_AUTH_SECRET=`
    - .env.example contains `BETTER_AUTH_URL=`
    - .env.example contains `NEXT_PUBLIC_APP_URL=`
    - .gitignore contains `.env.local`
    - vitest.config.ts contains `jsdom`
    - src/test/setup.ts contains `@testing-library/jest-dom`
    - `npm run lint` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire Better Auth, Drizzle db instance, API route, and generate schema + migration</name>
  <files>src/db/index.ts, src/db/schema.ts, src/lib/auth.ts, src/lib/auth-client.ts, src/app/api/auth/[...all]/route.ts, drizzle/ (migration SQL files)</files>
  <read_first>
    Read drizzle.config.ts (confirm schema output path is `./src/db/schema.ts` and out directory is `./drizzle`). Read package.json (confirm all required packages from Task 1 are installed before creating files that import them). Read tsconfig.json (confirm `@/*` path alias points to `./src/*` — all imports below use this alias).
  </read_first>
  <action>
    Create `src/db/index.ts`. Import `drizzle` from `drizzle-orm/postgres-js`. Import `postgres` from `postgres`. Import `* as schema` from `./schema`. Create the postgres client with `{ prepare: false }` — this flag is REQUIRED because DATABASE_URL points to the Supabase Transaction pooler (port 6543) which does not support prepared statements; omitting it causes runtime crashes on Vercel. Export `const db = drizzle(client, { schema })`.

    Create a temporary placeholder `src/db/schema.ts` that exports four `any`-typed constants to satisfy TypeScript imports until the Better Auth CLI overwrites this file. Export: `user`, `session`, `account`, `verification` — each typed as `any`. This file will be completely replaced in the next step.

    Create `src/lib/auth.ts`. Import `betterAuth` from `better-auth`. Import `drizzleAdapter` from `@better-auth/drizzle-adapter`. Import `username` from `better-auth/plugins`. Import `db` from `@/db`. Import `* as schema` from `@/db/schema`. Call `betterAuth` with these exact configuration values:
    - `baseURL: process.env.BETTER_AUTH_URL!`
    - `secret: process.env.BETTER_AUTH_SECRET!`
    - `database`: `drizzleAdapter(db, { provider: "pg", schema: { user: schema.user, session: schema.session, account: schema.account, verification: schema.verification } })`
    - `emailAndPassword: { enabled: true }`
    - `plugins: [username()]`
    - `session: { expiresIn: 60 * 60 * 5, updateAge: 60 * 30 }` — 5-hour idle timeout (D-05), 30-minute refresh granularity prevents unexpected logouts (per RESEARCH.md Pitfall 7)
    Export `auth` and export `type Session = typeof auth.$Infer.Session`.

    Create `src/lib/auth-client.ts`. Import `createAuthClient` from `better-auth/react`. Import `usernameClient` from `better-auth/client/plugins`. Call `createAuthClient` with `baseURL: process.env.NEXT_PUBLIC_APP_URL` and `plugins: [usernameClient()]`. Export as `authClient`. The `usernameClient()` plugin is REQUIRED — without it, `authClient.signIn.username` does not exist and the login form will throw at runtime (per RESEARCH.md Pitfall 2).

    Create directory `src/app/api/auth/[...all]/` and file `route.ts`. Import `toNextJsHandler` from `better-auth/next-js`. Import `auth` from `@/lib/auth`. Export `const { GET, POST } = toNextJsHandler(auth)`. Do NOT use `toNodeHandler` — that is for the Pages Router only.

    Run `npx @better-auth/cli generate --output src/db/schema.ts`. If the CLI asks for the auth config file path, specify `src/lib/auth.ts`. This command reads the Better Auth configuration and generates the correct Drizzle schema for all auth tables (user, session, account, verification). It will OVERWRITE the placeholder schema.ts created earlier — this is expected and correct. Inspect the generated schema.ts to confirm it exports `user`, `session`, `account`, and `verification` as Drizzle table definitions (not `any`).

    Run `npx drizzle-kit generate`. This reads `drizzle.config.ts` and the generated schema.ts, then creates a SQL migration file in the `drizzle/` directory. DATABASE_URL_DIRECT does NOT need to be set for this step — only for `drizzle-kit migrate` (which runs in Plan 02). Confirm that at least one `.sql` file appears in the `drizzle/` directory after this command.

    Run `npx tsc --noEmit` to confirm zero TypeScript errors across all created files. If errors exist, fix them before marking this task complete.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/db/index.ts contains `prepare: false`
    - src/db/index.ts contains `drizzle(client, { schema })`
    - src/lib/auth.ts contains `betterAuth(`
    - src/lib/auth.ts contains `expiresIn: 60 * 60 * 5`
    - src/lib/auth.ts contains `updateAge: 60 * 30`
    - src/lib/auth.ts contains `plugins: [username()]`
    - src/lib/auth.ts contains `drizzleAdapter(db`
    - src/lib/auth-client.ts contains `createAuthClient(`
    - src/lib/auth-client.ts contains `usernameClient()`
    - src/lib/auth-client.ts contains `NEXT_PUBLIC_APP_URL`
    - src/app/api/auth/[...all]/route.ts contains `toNextJsHandler(auth)`
    - src/db/schema.ts contains `export const user` (real Drizzle table, not `any`)
    - src/db/schema.ts contains `export const session`
    - src/db/schema.ts contains `export const account`
    - src/db/schema.ts contains `export const verification`
    - drizzle/ directory contains at least one .sql file
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Third-party packages installed from npm registry; supply chain attack surface |
| .env.local | Secrets file on developer machine; must never be committed to git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example / .env.local | mitigate | .env.example documents variable names only (no values). .env.local is in .gitignore. BETTER_AUTH_SECRET must never appear in any committed file. Verify .gitignore contains `.env.local` before first commit. |
| T-01-02 | Spoofing | BETTER_AUTH_SECRET length | mitigate | BETTER_AUTH_SECRET must be 32+ characters. Document in .env.example comment: "generate with: openssl rand -base64 32". Reject secrets shorter than 32 chars (Better Auth enforces minimum length at startup). |
| T-01-03 | Spoofing | prepare:false omission | mitigate | If `prepare: false` is omitted from src/db/index.ts, the app crashes with prepared statement errors in production (Supabase Transaction pooler incompatibility). Acceptance criterion explicitly checks for `prepare: false`. |
| T-01-SC | Tampering | npm package install supply chain | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit (all marked Approved, no [SLOP] or [SUS] packages). Postinstall scripts checked — none found for better-auth or @better-auth/drizzle-adapter. |
</threat_model>

<verification>
After both tasks complete, verify the full plan:

1. `npx tsc --noEmit` — must exit 0
2. `npm run lint` — must exit 0
3. `npm run build` — must complete without errors (even though .env.local does not exist yet, Next.js build should succeed — env vars are only required at runtime)
4. `ls drizzle/` — must show at least one .sql migration file
5. Inspect src/db/schema.ts — must have real Drizzle table exports (not `any` placeholder)
6. `grep -c "BETTER_AUTH_SECRET" .env.example` — must return 1 (documented but no value)
7. `grep ".env.local" .gitignore` — must return a match
</verification>

<success_criteria>
All auth infrastructure code is in place and type-checks cleanly. The project can be `npm run build` without errors. A SQL migration file is ready to apply in Plan 02. The developer can see from .env.example exactly which secrets need to be configured before the app can run.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md` when done.
</output>
