---
phase: 05-real-time-live-board
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/api/sessions/[sessionId]/pairs/route.ts
  - src/app/api/sessions/[sessionId]/pairs/route.test.ts
autonomous: true
requirements: [PAIR-05]

must_haves:
  truths:
    - "An authenticated GET to /api/sessions/[sessionId]/pairs returns the current pair list as JSON"
    - "An unauthenticated GET returns 401"
    - "The pair list shape matches the SSR getPairsForSession output (id + members with camper fields)"
  artifacts:
    - path: "src/app/api/sessions/[sessionId]/pairs/route.ts"
      provides: "GET Route Handler returning fresh pairs JSON"
      exports: ["GET"]
    - path: "src/app/api/sessions/[sessionId]/pairs/route.test.ts"
      provides: "Auth-gate + happy-path tests (V4 access control)"
  key_links:
    - from: "route.ts"
      to: "auth.api.getSession"
      via: "Better Auth session check, 401 on null"
      pattern: "auth\\.api\\.getSession"
    - from: "route.ts"
      to: "pairMember / camper tables"
      via: "drizzle select innerJoin filtered by sessionId"
      pattern: "pairMember"
---

<objective>
Create the `GET /api/sessions/[sessionId]/pairs` Route Handler that `LiveBoard` calls on every Realtime event and after every mutation. It validates the Better Auth session, queries pairs for the session, and returns them as JSON in the same shape the SSR page produces.

Purpose: D-01/D-02 — the refetch-on-event strategy needs a single auth-gated endpoint that always returns DB-consistent pair state.
Output: `route.ts` (GET) and its finalized test file (replacing the Wave 0 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-real-time-live-board/05-RESEARCH.md
@.planning/phases/05-real-time-live-board/05-PATTERNS.md

<interfaces>
<!-- SSR query to mirror exactly. From src/app/(protected)/pools/[poolId]/page.tsx getPairsForSession (lines 65-98) -->
Select { pairId, camperId, firstName, lastName, bunk, code } from pairMember innerJoin camper on pairMember.camperId = camper.id where pairMember.sessionId = sessionId. Group rows into Array<{ id: pairId, members: Array<{ camperId, firstName, lastName, bunk, code }> }>.

<!-- Auth pattern. From page.tsx lines 1-3, 111 and actions.ts requireAuth lines 11-17 -->
const authSession = await auth.api.getSession({ headers: await headers() }); — in a Route Handler return Response.json({ error: "Unauthorized" }, { status: 401 }) when null (NOT redirect).

<!-- Next.js 16 Route Handler signature -->
export async function GET(request: NextRequest, { params }: { params: Promise<{ sessionId: string }> }) — params is a Promise, MUST be awaited before destructuring.

<!-- Schema: pairMember has columns pair_id, camper_id, session_id (denormalized). camper has firstName, lastName, bunk, code. From src/db/schema.ts lines 158-184. -->

<!-- Route test mock pattern. From src/app/(protected)/pools/page.test.tsx -->
vi.mock("@/lib/auth", () => ({ auth: { api: { getSession: vi.fn() } } })); vi.mock("next/headers", ...); vi.mock("@/db", ...). Use vi.mocked(...).mockResolvedValueOnce(...) per test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize the Route Handler auth-gate + happy-path tests</name>
  <files>src/app/api/sessions/[sessionId]/pairs/route.test.ts</files>
  <read_first>
    - src/app/api/sessions/[sessionId]/pairs/route.test.ts (Wave 0 stub created in 05-01 — replace the it.todo cases)
    - src/app/(protected)/pools/page.test.tsx (mocking conventions for @/lib/auth, next/headers, @/db)
    - .planning/phases/05-real-time-live-board/05-VALIDATION.md (Route Handler row — V4 access control test)
  </read_first>
  <behavior>
    - GET with auth.api.getSession mocked to return null responds with status 401.
    - GET with auth.api.getSession mocked to return a user, and db returning two pair_member rows for one pairId, responds 200 with JSON { pairs: [ { id, members: [...] } ] } grouped correctly.
    - The awaited params promise resolves the sessionId used in the db where-clause.
  </behavior>
  <action>
    Replace the `it.todo` placeholders in `route.test.ts` with real tests for the two behaviors above, following page.test.tsx mock conventions: mock `@/lib/auth`, `next/headers`, and `@/db`. Invoke the exported `GET` directly with a fake `NextRequest` and a `params` argument shaped as `Promise.resolve({ sessionId: "s1" })`. Assert `response.status === 401` for the unauthenticated case, and assert the parsed JSON `pairs` array groups members by pairId for the authenticated case. Keep the `describe` label containing `V4 access control`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `route.test.ts` no longer contains `it.todo`
    - Tests assert 401 for unauthenticated and grouped pairs JSON for authenticated
    - `describe` label contains `V4 access control`
    - `npm test` exits 0 (tests fail until Task 2 implements route.ts — implement Task 2 to green them)
  </acceptance_criteria>
  <done>route.test.ts contains real auth-gate + happy-path tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GET /api/sessions/[sessionId]/pairs</name>
  <files>src/app/api/sessions/[sessionId]/pairs/route.ts</files>
  <read_first>
    - src/app/(protected)/pools/[poolId]/page.tsx (getPairsForSession query + grouping to mirror, lines 65-98)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (Combined Route Handler target pattern lines 112-162, Key conventions lines 156-162)
    - .planning/phases/05-real-time-live-board/05-RESEARCH.md (Pattern 3 + Pitfall 4 — await params; Security Domain V4/V5)
    - src/db/schema.ts (pair, pairMember, camper definitions lines 158-184)
  </read_first>
  <action>
    Create `src/app/api/sessions/[sessionId]/pairs/route.ts` exporting `async function GET(request: NextRequest, { params }: { params: Promise<{ sessionId: string }> })`. First `const { sessionId } = await params` (params is a Promise in Next.js 16 — destructuring without await throws). Then `const authSession = await auth.api.getSession({ headers: await headers() })` and `if (!authSession) return Response.json({ error: "Unauthorized" }, { status: 401 })` BEFORE any DB access (V4 access control). Then run the same drizzle select + innerJoin + groupBy-pairId logic as `getPairsForSession` in page.tsx (select pairId, camperId, firstName, lastName, bunk, code from pairMember innerJoin camper where pairMember.sessionId equals the awaited sessionId; build a Map keyed by pairId into `{ id, members: [...] }`). Return `Response.json({ pairs })` — use `Response.json`, not `NextResponse.json`. swimmerCount/pairCount are derived client-side (D-02 discretion) so are NOT included in the payload. A non-existent or foreign sessionId simply yields an empty `pairs` array (V5 — harmless, not an error). Do not add `export const dynamic`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `route.ts` exports `GET`
    - `route.ts` contains `await params` before destructuring `sessionId`
    - `route.ts` calls `auth.api.getSession` and returns `{ status: 401 }` when null, before any db query
    - `route.ts` queries `pairMember` innerJoin `camper` filtered by the awaited `sessionId`
    - `route.ts` returns `Response.json({ pairs })` and does NOT import `NextResponse`
    - `npm test` exits 0 with route.test.ts cases passing
  </acceptance_criteria>
  <done>GET handler validates auth, returns grouped pairs JSON, and its tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API (Route Handler) | Untrusted browser request with a session cookie crosses into server DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | GET /api/sessions/[sessionId]/pairs | mitigate | V4 Access Control: `auth.api.getSession({ headers: await headers() })` at the top of GET; return `Response.json({ error: "Unauthorized" }, { status: 401 })` before any DB query |
| T-05-02 | Tampering | sessionId path parameter | mitigate | V5 Input Validation: sessionId is taken only from awaited `params` and used solely as a parameterized drizzle where-clause value; an unknown/foreign sessionId returns an empty pairs array, never an error or another session's data is leaked beyond pair-membership data that is already non-sensitive |
| T-05-03 | Information Disclosure | pair data via anon-readable tables | accept | RLS skipped for v1 (STATE.md); the Route Handler itself is auth-gated by Better Auth, so the HTTP path requires a valid session |
</threat_model>

<verification>
- `npm test` green with route.test.ts passing (401 + grouped-JSON cases).
- Grep gates: route.ts contains `await params`, `auth.api.getSession`, `Response.json`, no `NextResponse`.
</verification>

<success_criteria>
- `GET /api/sessions/[sessionId]/pairs` returns 401 unauthenticated and grouped pairs JSON when authenticated.
- Payload shape matches the SSR getPairsForSession output.
</success_criteria>

<output>
Create `.planning/phases/05-real-time-live-board/05-03-SUMMARY.md` when done.
</output>
