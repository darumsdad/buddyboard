---
phase: 05-real-time-live-board
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/supabase-browser.ts
  - src/test/mocks/supabase.ts
  - src/app/(protected)/pools/[poolId]/components/LiveBoard.test.tsx
  - src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx
  - src/app/api/sessions/[sessionId]/pairs/route.test.ts
autonomous: false
requirements: [PAIR-05]
user_setup:
  - service: supabase
    why: "Realtime postgres_changes subscription needs the anon key, the public project URL, and both pair tables enabled on the supabase_realtime publication"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Known value https://oahkyfeogugoghhhtaoh.supabase.co (also at Supabase Dashboard -> Project Settings -> API)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon public"
    dashboard_config:
      - task: "Enable Realtime on pair_member AND pair tables in the supabase_realtime publication"
        location: "Supabase Dashboard -> Database -> Replication -> supabase_realtime (or SQL Editor: alter publication supabase_realtime add table pair_member; alter publication supabase_realtime add table pair;)"

must_haves:
  truths:
    - "A Supabase browser client can be imported as a singleton in any client component"
    - "Both pair_member and pair tables are members of the supabase_realtime publication"
    - "Wave 0 test scaffolding exists and npm test stays green"
  artifacts:
    - path: "src/lib/supabase-browser.ts"
      provides: "Singleton Supabase browser client"
      exports: ["supabase"]
    - path: "src/test/mocks/supabase.ts"
      provides: "Reusable vi.mock for @/lib/supabase-browser with capturable subscribe callback"
    - path: "src/app/(protected)/pools/[poolId]/components/LiveBoard.test.tsx"
      provides: "Stub test cases for BOARD-01, BOARD-02, BOARD-05"
    - path: "src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx"
      provides: "Stub test cases for BOARD-04"
    - path: "src/app/api/sessions/[sessionId]/pairs/route.test.ts"
      provides: "Stub test cases for Route Handler V4 auth gate"
  key_links:
    - from: "src/lib/supabase-browser.ts"
      to: "@supabase/supabase-js"
      via: "createClient import"
      pattern: "createClient"
---

<objective>
Lay the real-time foundation: install `@supabase/supabase-js`, create the singleton browser client, scaffold the Wave 0 test infrastructure, and complete the one-time manual Supabase setup (env vars + publication) that the subscription depends on.

Purpose: Every later Phase 5 plan consumes either the Supabase client, the test mock, or the manual setup performed here. Nothing real-time can function until both pair tables are on the `supabase_realtime` publication and the env vars are set.
Output: `supabase-browser.ts` singleton, `src/test/mocks/supabase.ts`, three Wave 0 stub test files, and a verified manual setup checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-real-time-live-board/05-RESEARCH.md
@.planning/phases/05-real-time-live-board/05-PATTERNS.md
@.planning/phases/05-real-time-live-board/05-VALIDATION.md

<interfaces>
<!-- Singleton-client analog the executor must mirror. From src/lib/auth-client.ts -->
src/lib/auth-client.ts exports a named singleton via `export const authClient = createAuthClient({...})`. No default export. The new supabase-browser.ts mirrors this exactly: `export const supabase = createClient(url, anonKey)`.

<!-- Test mock convention. From src/app/(protected)/pools/page.test.tsx -->
Tests use top-level vi.mock blocks, vi.clearAllMocks() in beforeEach, dynamic import + vi.mocked(...).mockResolvedValueOnce(...) inside each test, describe blocks named after the requirement ID.

<!-- Vitest config (root vitest.config.ts): environment jsdom, globals true, include src/**/*.test.{ts,tsx}, setupFiles src/test/setup.ts. Run with `npm test`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @supabase/supabase-js and create the singleton browser client</name>
  <files>package.json, src/lib/supabase-browser.ts</files>
  <read_first>
    - src/lib/auth-client.ts (singleton named-export pattern to mirror)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (supabase-browser.ts target content, lines 28-58)
    - .planning/phases/05-real-time-live-board/05-RESEARCH.md (Package Legitimacy Audit + Standard Stack — @supabase/supabase-js 2.108.2, slopcheck [OK] Approved)
    - package.json (current dependencies)
  </read_first>
  <action>
    Run `npm install @supabase/supabase-js` (audited [OK]/Approved in RESEARCH.md Package Legitimacy Audit — 8+ yr package, ~50M weekly downloads, no postinstall script; no install checkpoint required). Then create `src/lib/supabase-browser.ts` that imports `createClient` from `@supabase/supabase-js` and exports a named singleton const `supabase` initialized with `process.env.NEXT_PUBLIC_SUPABASE_URL!` and `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!`. Use `createClient` from `@supabase/supabase-js` — NOT `createBrowserClient` from `@supabase/ssr` (this project uses Better Auth, not Supabase Auth). No options object, no auth config. Use the `!` non-null assertion to match the project's lib-file env convention. Named export only, no default export.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/lib/supabase-browser.ts','utf8');if(!/export const supabase/.test(s))process.exit(1);if(!/from ['\"]@supabase\/supabase-js['\"]/.test(s))process.exit(1);if(/@supabase\/ssr/.test(s))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `@supabase/supabase-js`
    - `src/lib/supabase-browser.ts` contains `export const supabase`
    - `src/lib/supabase-browser.ts` imports `createClient` from `@supabase/supabase-js`
    - `src/lib/supabase-browser.ts` references `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`
    - `src/lib/supabase-browser.ts` does NOT import from `@supabase/ssr`
  </acceptance_criteria>
  <done>Package installed; supabase-browser.ts exports a named singleton built with createClient and the two NEXT_PUBLIC env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Wave 0 test infrastructure (Supabase mock + three stub test files)</name>
  <files>src/test/mocks/supabase.ts, src/app/(protected)/pools/[poolId]/components/LiveBoard.test.tsx, src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx, src/app/api/sessions/[sessionId]/pairs/route.test.ts</files>
  <read_first>
    - src/app/(protected)/pools/page.test.tsx (vi.mock / beforeEach / describe-per-requirement conventions)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (Testing Patterns — Supabase mock approach, lines 702-727)
    - .planning/phases/05-real-time-live-board/05-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - vitest.config.ts (include glob, setupFiles)
  </read_first>
  <action>
    Create `src/test/mocks/supabase.ts` exporting a helper that registers `vi.mock("@/lib/supabase-browser", ...)` returning a fake `supabase` object whose `channel()` returns a chainable object (`on()` returns `this`, `subscribe()` returns `this`) and which exposes a way to capture the subscribe status callback so tests can invoke it manually to simulate `SUBSCRIBED` / `CHANNEL_ERROR` / `CLOSED`. Include a `removeChannel` vi.fn(). Then create three STUB test files that keep `npm test` green by using `it.todo(...)` placeholders only — do NOT import the not-yet-created components/route (those imports would break the suite). The stubs:
    - `src/app/(protected)/pools/[poolId]/components/LiveBoard.test.tsx` — `describe("BOARD-01", ...)` with `it.todo("renders the SSR initialPairs snapshot immediately without a loading gap")`; `describe("BOARD-02", ...)` with `it.todo("derives swimmerCount as sum of member counts and pairCount as pair list length")`; `describe("BOARD-05", ...)` with `it.todo("shows 'No pairs checked in yet' when connected and empty")` and `it.todo("keeps last-known pairs visible when disconnected")`.
    - `src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx` — `describe("BOARD-04", ...)` with `it.todo("renders nothing when status is connected")`, `it.todo("shows Reconnecting message for reconnecting status")`, `it.todo("shows Disconnected message and Refresh button for disconnected status")`.
    - `src/app/api/sessions/[sessionId]/pairs/route.test.ts` — `describe("Route Handler V4 access control", ...)` with `it.todo("returns 401 when auth.api.getSession returns null")` and `it.todo("returns pairs JSON when authenticated")`.
    Each describe label must contain the exact requirement/threat token shown above so the gate greps succeed.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (green; todo cases reported as todo, not failing)
    - `src/test/mocks/supabase.ts` exists and references `@/lib/supabase-browser` and `removeChannel`
    - `LiveBoard.test.tsx` contains describe labels `BOARD-01`, `BOARD-02`, `BOARD-05`
    - `ConnectionBanner.test.tsx` contains describe label `BOARD-04`
    - `route.test.ts` contains describe label matching `V4 access control`
    - None of the three stub files import `./LiveBoard`, `./ConnectionBanner`, or `../pairs/route` (imports added in later plans)
  </acceptance_criteria>
  <done>Supabase mock utility plus three it.todo stub files exist; npm test is green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Complete manual Supabase setup (env vars + dual-table publication)</name>
  <action>
    BLOCKING human action — Claude cannot perform these steps (the anon key is a secret obtained from the Dashboard, and toggling the supabase_realtime publication requires Dashboard/SQL access). The human operator completes the env-var and publication steps described in <how-to-verify> below.
  </action>
  <verify>
    <human-check>Both NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY are set locally and on Vercel, and `select tablename from pg_publication_tables where pubname='supabase_realtime';` returns both `pair` and `pair_member`.</human-check>
  </verify>
  <done>Env vars set (local + Vercel) and both pair and pair_member are members of the supabase_realtime publication.</done>
  <what-built>The Supabase client singleton and test scaffolding are in place, but the live subscription cannot receive events until two manual Supabase steps are done. These require Dashboard access and a secret anon key that Claude cannot obtain.</what-built>
  <how-to-verify>
    1. In the Supabase Dashboard for project `oahkyfeogugoghhhtaoh`, go to Project Settings -> API and copy the `anon` `public` key.
    2. Add to local `.env.local` (create if missing): `NEXT_PUBLIC_SUPABASE_URL=https://oahkyfeogugoghhhtaoh.supabase.co` and `NEXT_PUBLIC_SUPABASE_ANON_KEY=<the anon key>`.
    3. Add the same two variables to Vercel: Project -> Settings -> Environment Variables (Production + Preview + Development).
    4. Enable Realtime on BOTH tables. Either toggle `pair_member` and `pair` on under Database -> Replication -> supabase_realtime, OR run in the SQL Editor: `alter publication supabase_realtime add table pair_member;` and `alter publication supabase_realtime add table pair;`. BOTH tables are mandatory — cascade-deleted pair_member rows do not fire Realtime events, so pair removals are detected via the `pair` table (RESEARCH.md Critical Finding #1 / Pitfall 5).
    5. Confirm by querying: `select tablename from pg_publication_tables where pubname = 'supabase_realtime';` — the result must include both `pair` and `pair_member`.
  </how-to-verify>
  <resume-signal>Type "approved" once both env vars are set (local + Vercel) and both pair and pair_member appear in the supabase_realtime publication, or describe what is blocking.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | New third-party package enters the dependency tree |
| browser -> Supabase Realtime | Anon key shipped to the client establishes a WebSocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm install @supabase/supabase-js | mitigate | Package audited in RESEARCH.md Package Legitimacy Audit: 8+ yr age, ~50M wk downloads, official supabase/supabase-js repo, slopcheck [OK], no postinstall script. Not [ASSUMED]/[SUS] — no blocking-human legitimacy checkpoint required |
| T-05-03 | Information Disclosure | NEXT_PUBLIC_SUPABASE_ANON_KEY in client bundle | accept | NEXT_PUBLIC_* is intentionally public; anon key only enables reading pair data with RLS skipped for v1; auth enforcement lives at the Next.js/Better Auth layer (STATE.md decision) |
</threat_model>

<verification>
- `npm test` green after Task 2.
- supabase-browser.ts grep gates pass (Task 1 automated check).
- Manual checkpoint approved: both env vars set locally and on Vercel; both `pair` and `pair_member` in the `supabase_realtime` publication.
</verification>

<success_criteria>
- `@supabase/supabase-js` installed and `src/lib/supabase-browser.ts` exports the singleton.
- Wave 0 mock + three stub test files exist; `npm test` exits 0.
- Manual Supabase setup (env vars + dual-table publication) confirmed.
</success_criteria>

<output>
Create `.planning/phases/05-real-time-live-board/05-01-SUMMARY.md` when done.
</output>
