---
phase: 05-real-time-live-board
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - src/app/(protected)/pools/[poolId]/page.tsx
autonomous: false
requirements: [PAIR-05, BOARD-01]

must_haves:
  truths:
    - "page.tsx renders <LiveBoard> instead of <SessionBoard>, passing the SSR pair snapshot"
    - "The page no longer runs the swimmerCount/pairCount DB count queries (counts are derived client-side)"
    - "Two browser tabs on the same pool see each other's add/remove pair changes within seconds without refresh"
  artifacts:
    - path: "src/app/(protected)/pools/[poolId]/page.tsx"
      provides: "Server page handing the SSR snapshot to LiveBoard"
      contains: "LiveBoard"
  key_links:
    - from: "page.tsx"
      to: "LiveBoard"
      via: "renders <LiveBoard initialPairs={pairs} .../>"
      pattern: "<LiveBoard"
---

<objective>
Wire `LiveBoard` into the session page: replace `<SessionBoard>` with `<LiveBoard>`, drop the now-redundant server-side count queries, and run the manual multi-device verification that proves PAIR-05 end to end.

Purpose: D-04 — page.tsx is the SSR-to-client seam. After this plan the live board is fully active for counselors.
Output: modified `page.tsx` and a confirmed multi-tab real-time verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-real-time-live-board/05-PATTERNS.md
@.planning/phases/05-real-time-live-board/05-VALIDATION.md

<interfaces>
<!-- Current page.tsx (src/app/(protected)/pools/[poolId]/page.tsx) -->
Imports SessionBoard. Computes `pairs = await getPairsForSession(session.id)` (line 130). Computes swimmerCount + pairCount via two count queries in Promise.all (lines 133-144). Renders JoinSessionModal (conditional) + <SessionBoard poolName swimmerCount pairCount sessionId poolId pairs /> (lines 151-163).

<!-- LiveBoard (Plan 05-05) props: { initialPairs: Pair[]; sessionId: string; poolId: string; poolName: string }. -->
<!-- getPairsForSession stays in page.tsx unchanged — still needed for the SSR snapshot. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SessionBoard with LiveBoard in page.tsx and drop count queries</name>
  <files>src/app/(protected)/pools/[poolId]/page.tsx</files>
  <read_first>
    - src/app/(protected)/pools/[poolId]/page.tsx (full file)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (page.tsx delta lines 417-457)
    - src/app/(protected)/pools/[poolId]/components/LiveBoard.tsx (props contract from 05-05)
  </read_first>
  <action>
    In `page.tsx`: replace `import { SessionBoard } from "./components/SessionBoard"` with `import { LiveBoard } from "./components/LiveBoard"`. Remove the Step 5 swimmerCount/pairCount `Promise.all` count block (current lines 133-144) — these are now derived client-side by LiveBoard (D-02). Remove the now-unused `pair` and `sql` imports if they are no longer referenced elsewhere in the file (verify `pair` and `sql` usages first; `getOrCreateActiveSession`/`getPairsForSession` use `poolSession`, `pairMember`, `camper`, `eq`, `and` — confirm before deleting). Keep `getPairsForSession` and the `pairs = await getPairsForSession(session.id)` call (line 130) — the SSR snapshot. Replace the `<SessionBoard ... />` JSX with `<LiveBoard initialPairs={pairs} sessionId={session.id} poolId={poolId} poolName={poolRecord[0].name} />`. Keep the `JoinSessionModal` conditional and the surrounding fragment unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(protected)/pools/[poolId]/page.tsx','utf8');if(!/<LiveBoard/.test(s))process.exit(1);if(/<SessionBoard/.test(s))process.exit(1);if(/from \".\/components\/SessionBoard\"/.test(s))process.exit(1);console.log('ok')" && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports `LiveBoard` and no longer imports `SessionBoard`
    - `page.tsx` renders `<LiveBoard initialPairs={pairs} sessionId={session.id} poolId={poolId} poolName={poolRecord[0].name} />`
    - `page.tsx` no longer contains the swimmerCount/pairCount count queries
    - `pairs = await getPairsForSession(session.id)` remains
    - `npx tsc --noEmit` reports no errors and `npm test` exits 0
  </acceptance_criteria>
  <done>page.tsx hands the SSR snapshot to LiveBoard; count queries removed; types and tests clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Multi-device real-time verification (PAIR-05, BOARD-04, BOARD-05)</name>
  <action>
    BLOCKING human verification — multi-device WebSocket propagation and the disconnected banner require two live browser sessions and a real network disruption, which cannot be unit tested. The human operator performs the two-tab test described in <how-to-verify> below.
  </action>
  <verify>
    <human-check>Adds, trios, and removes performed in Tab A appear in Tab B within seconds without refresh; the Disconnected banner appears on WebSocket drop and clears on restore.</human-check>
  </verify>
  <done>Add/trio/remove propagate across two tabs within seconds and the disconnected banner behaves per spec.</done>
  <what-built>The full real-time live board is wired: page.tsx renders LiveBoard, which subscribes to Supabase Realtime and re-fetches via the Route Handler on each event. Multi-device propagation (PAIR-05) and the connection banner (BOARD-04) require two live WebSocket connections and a real network disruption, so they cannot be unit tested.</what-built>
  <how-to-verify>
    1. Ensure the dev server is running (`npm run dev`) and you are logged in. Confirm the Plan 05-01 manual setup is done (env vars set, both `pair` and `pair_member` on the supabase_realtime publication) — without it, events will not arrive.
    2. Open the SAME pool session in two browser tabs (Tab A and Tab B).
    3. In Tab A, add a buddy pair. Within ~2 seconds Tab B should show the new pair AND the header count update — with no manual refresh (PAIR-05, BOARD-01, BOARD-02).
    4. In Tab A, add a trio. Tab B should show the trio as a single row (the 150ms debounce collapses the 3 INSERT events into one refetch — confirm only one updated row appears, no flicker of partial pairs).
    5. In Tab A, remove a pair. Within ~2 seconds Tab B should drop that pair and update counts (this exercises the `pair` DELETE subscription — the critical dual-table path).
    6. In Tab B DevTools -> Network, throttle/block the WebSocket (or go offline). The "Disconnected — data may be stale." banner should appear with a working Refresh button; the last-known pairs stay visible (BOARD-04, BOARD-05 State B). Restore the network and confirm the banner clears.
    7. With an empty session while connected, confirm the board shows "No pairs checked in yet" and NO banner (BOARD-05 State A).
  </how-to-verify>
  <resume-signal>Type "approved" once add, trio, and remove all propagate across both tabs within seconds and the disconnected banner behaves correctly, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> SSR page | Authenticated request renders the session page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | page.tsx (Server Component) | mitigate | Unchanged auth gate — page.tsx still calls `auth.api.getSession` and `redirect("/login")` when unauthenticated, and `redirect("/pools")` for an unknown poolId, before rendering LiveBoard |
| T-05-09 | Information Disclosure | initial SSR pair snapshot | accept | initialPairs is the same auth-gated data the page already rendered in Phase 4; passing it as a prop to a client component does not widen exposure beyond the authenticated user's own pool view |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test` green.
- Grep gates: page.tsx renders `<LiveBoard`, no `<SessionBoard`.
- Manual multi-tab checkpoint approved (add/trio/remove propagation + disconnected banner).
</verification>

<success_criteria>
- page.tsx renders LiveBoard with the SSR snapshot; redundant count queries removed.
- Multi-device add/remove propagation verified within seconds without refresh (PAIR-05).
- Connection banner and empty-state behavior confirmed live (BOARD-04, BOARD-05).
</success_criteria>

<output>
Create `.planning/phases/05-real-time-live-board/05-06-SUMMARY.md` when done.
</output>
