---
phase: 05-real-time-live-board
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/(protected)/pools/[poolId]/components/ConnectionBanner.tsx
  - src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx
  - src/app/(protected)/pools/[poolId]/components/PairSkeleton.tsx
  - src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx
  - src/app/(protected)/pools/[poolId]/components/PairList.tsx
autonomous: true
requirements: [BOARD-04, BOARD-05]

must_haves:
  truths:
    - "ConnectionBanner renders nothing when connected, a Reconnecting message when reconnecting, and a Disconnected message with a Refresh button when disconnected"
    - "PairSkeleton renders exactly 3 animated placeholder rows"
    - "SessionBoard threads a pair-mutation callback and a refreshing flag down to AddPairForm and PairList"
    - "PairList shows the skeleton while refreshing and the empty state when connected with no pairs"
  artifacts:
    - path: "src/app/(protected)/pools/[poolId]/components/ConnectionBanner.tsx"
      provides: "Connection status banner (BOARD-04)"
      min_lines: 20
    - path: "src/app/(protected)/pools/[poolId]/components/PairSkeleton.tsx"
      provides: "3-row loading skeleton"
    - path: "src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx"
      provides: "Presentational board threading live props"
    - path: "src/app/(protected)/pools/[poolId]/components/PairList.tsx"
      provides: "Pair list with skeleton + empty state (BOARD-05)"
  key_links:
    - from: "SessionBoard.tsx"
      to: "AddPairForm onSuccess / PairList onPairRemoved"
      via: "onPairMutated prop threaded down"
      pattern: "onPairMutated"
    - from: "PairList.tsx"
      to: "PairSkeleton"
      via: "rendered when isRefreshing"
      pattern: "PairSkeleton"
---

<objective>
Build the two new presentational components (`ConnectionBanner`, `PairSkeleton`) and upgrade `SessionBoard` + `PairList` to thread the live props that `LiveBoard` will drive: a `onPairMutated` refresh callback and an `isRefreshing` flag. This is the visible UI layer for connection status (BOARD-04) and the distinct empty/loading states (BOARD-05).

Purpose: D-07/D-08/D-10/D-11 — SessionBoard becomes fully presentational; the banner and skeleton give the counselor clear connection and loading feedback.
Output: ConnectionBanner + its test, PairSkeleton, and prop-threaded SessionBoard + PairList.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-real-time-live-board/05-UI-SPEC.md
@.planning/phases/05-real-time-live-board/05-PATTERNS.md

<interfaces>
<!-- ConnectionBanner contract. From 05-UI-SPEC.md Component Inventory #2 -->
Props: { status: "connected" | "reconnecting" | "disconnected"; onRefresh: () => void }.
- connected -> render null (display none, not opacity-0; no layout shift).
- reconnecting -> bg-slate-100 border-b border-slate-200 px-4 py-2 flex items-center gap-2; Loader2 size 16 animate-spin text-slate-600 aria-hidden; text-sm text-slate-600 "Reconnecting…"; NO refresh button.
- disconnected -> bg-amber-50 border-b border-amber-200 px-4 min-h-[44px] flex items-center justify-between; text-sm text-amber-900 "Disconnected — data may be stale."; button text-sm font-semibold text-amber-900 underline min-h-[44px] px-2 with aria-label "Refresh pair list" and visible label "Refresh", onClick=onRefresh.
- Outer element sticky top-[57px] z-9 (header is sticky top-0 z-10), role="status".

<!-- PairSkeleton contract. From 05-UI-SPEC.md #3 + PATTERNS lines 385-405 -->
Pure presentational (no "use client"). Wrapper animate-pulse divide-y divide-slate-200; exactly 3 rows, each flex items-center justify-between px-4 py-3 with a h-4 bg-slate-200 rounded-md w-3/5 bar and a w-10 h-10 bg-slate-200 rounded-full circle; each row aria-hidden="true".

<!-- SessionBoard current props: { poolName, swimmerCount, pairCount, sessionId, poolId, pairs }. Add onPairMutated: () => void and isRefreshing: boolean. Stays a server/presentational component (no "use client"). -->

<!-- PairList current props: { pairs, sessionId, poolId }. Add onPairRemoved: () => void and isRefreshing: boolean. Empty-state copy already present: "No pairs checked in yet" / "Use the form above to add the first buddy pair." -->

<!-- AddPairForm (after 05-02) now requires onSuccess: () => void. PairRow (after 05-02) now requires onRemoved: () => void. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConnectionBanner + tests</name>
  <files>src/app/(protected)/pools/[poolId]/components/ConnectionBanner.tsx, src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx</files>
  <read_first>
    - .planning/phases/05-real-time-live-board/05-UI-SPEC.md (Component Inventory #2, Accessibility, Copywriting)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (ConnectionBanner target pattern lines 296-360)
    - src/app/(protected)/pools/[poolId]/components/ConnectionBanner.test.tsx (Wave 0 stub from 05-01 — replace it.todo cases)
    - src/app/(protected)/pools/[poolId]/components/PairRow.tsx ("use client" + lucide-react import convention)
  </read_first>
  <behavior>
    - status "connected" -> component renders null (no DOM output).
    - status "reconnecting" -> renders text "Reconnecting…", no button with name /refresh/i.
    - status "disconnected" -> renders text "Disconnected — data may be stale." and a button accessible by name "Refresh pair list"; clicking it calls onRefresh once.
  </behavior>
  <action>
    Create `ConnectionBanner.tsx` as a `"use client"` component with props `{ status: "connected" | "reconnecting" | "disconnected"; onRefresh: () => void }`. Return `null` when `status === "connected"` (D-08 — hidden entirely, no visual noise). Reconnecting branch: outer `div` `sticky top-[57px] z-9 bg-slate-100 border-b border-slate-200 px-4 py-2 flex items-center gap-2` with `role="status"`, a `Loader2` icon (`size={16}`, `className="animate-spin text-slate-600"`, `aria-hidden="true"`) imported from `lucide-react`, and a `text-sm text-slate-600` span reading `Reconnecting…`; no Refresh button. Disconnected branch: outer `div` `sticky top-[57px] z-9 bg-amber-50 border-b border-amber-200 px-4 min-h-[44px] flex items-center justify-between` with `role="status"`, a `text-sm text-amber-900` span reading `Disconnected — data may be stale.`, and a `button` (`type="button"`, `onClick={onRefresh}`, `aria-label="Refresh pair list"`, `className="text-sm font-semibold text-amber-900 underline min-h-[44px] px-2"`) with visible text `Refresh`. Then replace the it.todo cases in ConnectionBanner.test.tsx with the three behaviors above.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ConnectionBanner.tsx` starts with `"use client"` and returns `null` for the connected status
    - Disconnected render contains the exact string `Disconnected — data may be stale.` and a button with `aria-label="Refresh pair list"`
    - Reconnecting render contains `Reconnecting…` and a `Loader2` with `aria-hidden`
    - Outer elements use `role="status"` and `top-[57px]`
    - ConnectionBanner.test.tsx has no `it.todo`; `npm test` exits 0
  </acceptance_criteria>
  <done>ConnectionBanner renders the three states per spec; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Create PairSkeleton</name>
  <files>src/app/(protected)/pools/[poolId]/components/PairSkeleton.tsx</files>
  <read_first>
    - .planning/phases/05-real-time-live-board/05-UI-SPEC.md (Component Inventory #3)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (PairSkeleton target lines 385-412)
    - src/app/(protected)/pools/[poolId]/components/PairRow.tsx (row layout to mirror dimensions)
  </read_first>
  <action>
    Create `PairSkeleton.tsx` as a pure presentational component (NO `"use client"` needed). Render a wrapper `div` `animate-pulse divide-y divide-slate-200` containing exactly 3 rows (map over `[1, 2, 3]`). Each row: `div` `flex items-center justify-between px-4 py-3` with `aria-hidden="true"`, containing a `div` `h-4 bg-slate-200 rounded-md w-3/5` (the name bar) and a `div` `w-10 h-10 bg-slate-200 rounded-full` (the remove-button placeholder). Matches PairRow row padding (`px-4 py-3`) and the remove-button circle dimensions (`w-10 h-10 rounded-full`). Named export.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(protected)/pools/[poolId]/components/PairSkeleton.tsx','utf8');if(!/animate-pulse/.test(s))process.exit(1);if((s.match(/aria-hidden/g)||[]).length<1)process.exit(1);if(!/rounded-full/.test(s))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `PairSkeleton.tsx` exports `PairSkeleton`
    - Contains `animate-pulse`, `divide-y divide-slate-200`, and renders 3 rows
    - Each row has `aria-hidden="true"`
    - Contains a `w-10 h-10 ... rounded-full` placeholder and a `h-4 bg-slate-200` bar
  </acceptance_criteria>
  <done>PairSkeleton renders 3 decorative animated rows matching PairRow dimensions.</done>
</task>

<task type="auto">
  <name>Task 3: Thread live props through SessionBoard and PairList</name>
  <files>src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx, src/app/(protected)/pools/[poolId]/components/PairList.tsx</files>
  <read_first>
    - src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx (full file — props + JSX)
    - src/app/(protected)/pools/[poolId]/components/PairList.tsx (full file — props + empty state)
    - src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx (now needs onSuccess — from 05-02)
    - src/app/(protected)/pools/[poolId]/components/PairRow.tsx (now needs onRemoved — from 05-02)
    - .planning/phases/05-real-time-live-board/05-PATTERNS.md (SessionBoard delta lines 461-491)
    - .planning/phases/05-real-time-live-board/05-UI-SPEC.md (Empty Board States A/B/C)
  </read_first>
  <action>
    SessionBoard: add `onPairMutated: () => void` and `isRefreshing: boolean` to `SessionBoardProps`, accept them in the destructured params, pass `onSuccess={onPairMutated}` to `<AddPairForm>`, and pass `onPairRemoved={onPairMutated}` and `isRefreshing={isRefreshing}` to `<PairList>`. Keep SessionBoard a presentational component with NO `"use client"` directive (LiveBoard is the client boundary). Do not change SessionHeader wiring (it already receives swimmerCount/pairCount as props). PairList: add `onPairRemoved: () => void` and `isRefreshing: boolean` to `PairListProps`. When `isRefreshing` is true, render `<PairSkeleton />` (imported from `./PairSkeleton`) instead of the pair rows / empty state (UI-SPEC State C). Otherwise keep the existing empty-state block when `pairs.length === 0` (State A — copy unchanged) and the existing mapped `<PairRow>` list, passing `onRemoved={onPairRemoved}` to each `<PairRow>`. Keep `divide-y divide-slate-200` wrapper.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `SessionBoard.tsx` props include `onPairMutated` and `isRefreshing`, passes `onSuccess={onPairMutated}` to AddPairForm and `onPairRemoved` + `isRefreshing` to PairList
    - `SessionBoard.tsx` has NO `"use client"` directive
    - `PairList.tsx` renders `<PairSkeleton />` when `isRefreshing` is true
    - `PairList.tsx` passes `onRemoved={onPairRemoved}` to each `<PairRow>`
    - `PairList.tsx` still renders the `No pairs checked in yet` empty state when not refreshing and empty
    - `npm test` exits 0 and `npx tsc --noEmit` reports no new errors in these files
  </acceptance_criteria>
  <done>SessionBoard and PairList thread the live callback + refreshing flag; skeleton shows during refresh; empty state preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side presentational components; no new data crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | ConnectionBanner / PairList stale data display | accept | Showing last-known pairs while disconnected (D-09 / UI-SPEC State B) is intentional UX; the banner explicitly warns "data may be stale", and no new data is fetched without the auth-gated Route Handler |
| T-05-07 | Tampering | Refresh button onRefresh handler | accept | onRefresh only triggers the same auth-gated Route Handler refetch wired by LiveBoard (Plan 05-05); no client-supplied parameters reach the server |
</threat_model>

<verification>
- `npm test` green including ConnectionBanner tests.
- Grep gates pass for PairSkeleton (animate-pulse, aria-hidden, rounded-full).
- SessionBoard has no "use client"; PairList renders PairSkeleton when isRefreshing.
</verification>

<success_criteria>
- ConnectionBanner correctly renders connected (null) / reconnecting / disconnected states with accessible Refresh button.
- PairSkeleton renders 3 decorative rows.
- SessionBoard + PairList thread onPairMutated/isRefreshing and show skeleton vs empty state distinctly.
</success_criteria>

<output>
Create `.planning/phases/05-real-time-live-board/05-04-SUMMARY.md` when done.
</output>
