---
phase: 04-sessions-and-buddy-pairs
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/app/(protected)/pools/[poolId]/components/LogoutButton.tsx
  - src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx
  - src/app/(protected)/pools/[poolId]/components/PairList.tsx
  - src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx
  - src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx
autonomous: true
requirements:
  - PAIR-01
  - PAIR-02
  - PAIR-03
  - SESS-02

must_haves:
  truths:
    - "AddPairForm renders two CamperField instances; the Add pair button is disabled until both campers are resolved"
    - "Submitting AddPairForm calls addPairAction and on success clears both fields and returns focus to the Camper 1 input"
    - "When addPairAction returns PAIR-04, AddPairForm passes the error to the offending CamperField's error prop"
    - "PairList renders divide-y PairRow instances; when empty it shows 'No pairs checked in yet' with body copy"
    - "SessionHeader is sticky and displays pool name, swimmer/pair hero count, CloseSessionDialog trigger, and Log out link"
    - "SessionBoard composes SessionHeader + AddPairForm + PairList in the correct layout order"
    - "LogoutButton calls authClient.signOut() and pushes /login; works in both SessionHeader and pools/page.tsx"
  artifacts:
    - path: "src/app/(protected)/pools/[poolId]/components/LogoutButton.tsx"
      provides: "authClient.signOut() + redirect to /login"
    - path: "src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx"
      provides: "Two-field pair entry form with PAIR-04 error routing"
    - path: "src/app/(protected)/pools/[poolId]/components/PairList.tsx"
      provides: "Scrollable pair list with empty state"
    - path: "src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx"
      provides: "Sticky header with hero count and session controls"
    - path: "src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx"
      provides: "Full session layout composition"
  key_links:
    - from: "AddPairForm"
      to: "CamperField"
      via: "onResolved callback sets camper1/camper2 state; inputRef passed to Camper 1 for focus restore"
      pattern: "onResolved.*setCamper"
    - from: "SessionHeader"
      to: "CloseSessionDialog"
      via: "rendered inside header with activePairCount prop from server"
      pattern: "CloseSessionDialog.*activePairCount"
    - from: "SessionBoard"
      to: "SessionHeader, AddPairForm, PairList"
      via: "pure composition — passes session/pair data as props"
      pattern: "SessionHeader.*AddPairForm.*PairList"
---

<objective>
Create the four composite UI components and the LogoutButton utility, then compose them into SessionBoard — the complete session screen layout.

Purpose: Wave 4 assembles the leaf components from Wave 3 into the full counselor-facing session interface. After this plan, the session page (Wave 5) only needs to fetch data and render SessionBoard.

Output: LogoutButton, AddPairForm, PairList, SessionHeader, SessionBoard components — all in src/app/(protected)/pools/[poolId]/components/.
</objective>

<execution_context>
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-RESEARCH.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-PATTERNS.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-UI-SPEC.md
</execution_context>

<context>
@C:\Users\darum\WebstormProjects\buddyboard\.planning\PROJECT.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\ROADMAP.md

<interfaces>
<!-- From Wave 3 leaf components (plan 04-03) -->

<!-- CamperField props -->
type CamperFieldProps = {
  sessionId: string;
  label: string;
  onResolved: (camper: CamperSuggestion | null) => void;
  error?: string | null;
  inputRef?: React.RefObject<HTMLInputElement>;
};

<!-- PairRow props -->
type PairRowProps = {
  pair: { id: string; members: Array<{ camperId: string; firstName: string; lastName: string; bunk: string }> };
  sessionId: string;
  poolId: string;
};

<!-- CloseSessionDialog props -->
type CloseSessionDialogProps = {
  sessionId: string;
  poolId: string;
  activePairCount: number;
};

<!-- From plan 04-02 (actions.ts) -->
export async function addPairAction(sessionId: string, poolId: string, camper1Id: string, camper2Id: string): Promise<AddPairResult>;

<!-- From src/lib/auth-client.ts (existing) -->
import { authClient } from "@/lib/auth-client";
// authClient.signOut() — browser-only, must be "use client"

<!-- Existing pattern: src/app/(protected)/pools/page.tsx lines 11-14 -->
{session.user.role === "admin" && (
  <Link href="/admin/users" className="absolute top-4 right-4 text-sm text-slate-600 hover:text-slate-900">Admin</Link>
)}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LogoutButton, AddPairForm, PairList</name>
  <files>
    src/app/(protected)/pools/[poolId]/components/LogoutButton.tsx
    src/app/(protected)/pools/[poolId]/components/AddPairForm.tsx
    src/app/(protected)/pools/[poolId]/components/PairList.tsx
  </files>
  <read_first>
    - src/app/(auth)/login/page.tsx — form submit pattern (lines 8-35), button disabled pattern (lines 82-88), error display (lines 78-80)
    - src/lib/auth-client.ts — confirm authClient export name; check if signOut is a method on the client
    - src/app/(admin)/admin/campers/page.tsx — empty state pattern (list + "No {items} yet" heading + body copy)
    - .planning/phases/04-sessions-and-buddy-pairs/04-PATTERNS.md — sections for AddPairForm, PairList
    - .planning/phases/04-sessions-and-buddy-pairs/04-UI-SPEC.md — Component 4 (AddPairForm layout and button classes), Component 7 (PairList container), Copywriting Contract
  </read_first>
  <action>
LogoutButton.tsx — "use client".
Props: `{ className?: string }` (allows pools/page.tsx to pass a custom class).
Imports: authClient from "@/lib/auth-client"; useRouter from "next/navigation".
Renders a `<button>` with default className `"text-sm text-slate-600 hover:text-slate-900"` (overridable via className prop) with text "Log out".
onClick handler: async, calls `await authClient.signOut()`, then `router.push("/login")`.
No loading state needed (redirect navigates away).

AddPairForm.tsx — "use client".
Props: `{ sessionId: string; poolId: string }`
Imports: useState, useRef, useTransition from "react"; addPairAction, CamperSuggestion from "../actions"; CamperField from "./CamperField".

State: camper1 (CamperSuggestion | null), camper2 (CamperSuggestion | null), pair04Error (string | null — which camper caused PAIR-04: "camper1" | "camper2" | null), genericError (string | null).
camper1Ref: useRef<HTMLInputElement>(null) — passed to the Camper 1 CamperField as inputRef for focus restoration per D-05.

isPending + startTransition from useTransition.

Form container: `<form onSubmit={handleSubmit} className="bg-slate-50 rounded-md p-4">`.
Layout inside form: on mobile `flex flex-col gap-4`; on md+ `md:flex md:gap-3 md:items-end` (two CamperField columns + button in a row).

Camper 1 CamperField: label="Camper 1", onResolved sets camper1 state, error prop = pair04Error==="camper1" ? the PAIR-04 error message : null, inputRef=camper1Ref.
Camper 2 CamperField: label="Camper 2", onResolved sets camper2 state, error prop = pair04Error==="camper2" ? the PAIR-04 error message : null.

Add pair button: `min-h-[44px] px-6 bg-blue-600 hover:bg-blue-700 text-white text-base font-semibold rounded-md disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-150`. disabled when `isPending || !camper1 || !camper2`. aria-disabled="true" when disabled. Text: isPending ? "Adding…" : "Add pair".

handleSubmit: e.preventDefault(); if (!camper1 || !camper2) return; clear errors; startTransition(async () => { const result = await addPairAction(sessionId, poolId, camper1.id, camper2.id); if result.success: setCamper1(null), setCamper2(null), camper1Ref.current?.focus(); else if result.error === "PAIR-04": figure out which camper caused the conflict (for now, show error on both fields by setting pair04Error to "both", and display "[Camper name] is already paired in this session. Choose a different camper." — since we can't know which camper violated without a conflictCamperId, show a generic PAIR-04 message below the form); else setGenericError(result.error). });

Generic server error: `{genericError && <p role="alert" className="text-base text-red-600 mt-2">{genericError}</p>}` below the button.
PAIR-04 display: since AddPairResult does not include conflictCamperId, show PAIR-04 as a generic error below the form with the text: "One of these campers is already in an active pair in this session. Choose a different camper." (role="alert" text-base text-red-600).

PairList.tsx — Server Component (no "use client" needed — it receives pairs as props).
Props: `{ pairs: Array<{ id: string; members: Array<{ camperId: string; firstName: string; lastName: string; bunk: string }> }>; sessionId: string; poolId: string }`
Imports: PairRow from "./PairRow".
If pairs.length === 0: render `<div className="py-12 text-center"><p className="text-base font-semibold text-slate-900">No pairs checked in yet</p><p className="text-base text-slate-500 mt-2">Use the form above to add the first buddy pair.</p></div>`.
If pairs.length > 0: render `<div className="divide-y divide-slate-200">` with a PairRow for each pair.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - LogoutButton.tsx begins with `"use client";` and calls `authClient.signOut()` before `router.push("/login")`
    - AddPairForm.tsx: Add pair button has `disabled={isPending || !camper1 || !camper2}` logic
    - AddPairForm.tsx: on success, calls `camper1Ref.current?.focus()` (D-05)
    - AddPairForm.tsx: button text switches between "Add pair" and "Adding…" based on isPending
    - PairList.tsx: empty state heading is exactly "No pairs checked in yet"
    - PairList.tsx: empty state body is exactly "Use the form above to add the first buddy pair."
    - PairList.tsx: non-empty state uses `divide-y divide-slate-200` container
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>LogoutButton, AddPairForm, and PairList compile with no TypeScript errors. AddPairForm correctly wires onResolved callbacks to camper state and the Add pair button disabled logic.</done>
</task>

<task type="auto">
  <name>Task 2: SessionHeader and SessionBoard</name>
  <files>
    src/app/(protected)/pools/[poolId]/components/SessionHeader.tsx
    src/app/(protected)/pools/[poolId]/components/SessionBoard.tsx
  </files>
  <read_first>
    - src/app/(protected)/pools/page.tsx — existing top-bar pattern (absolute top-4 right-4 for Admin link); LogoutButton import pattern
    - .planning/phases/04-sessions-and-buddy-pairs/04-PATTERNS.md — sections for SessionHeader and SessionBoard
    - .planning/phases/04-sessions-and-buddy-pairs/04-UI-SPEC.md — Component 3 (SessionHeader layout and hero count classes), Page Layout Contract (/pools/[poolId] ASCII diagram), Typography table (36px text-4xl for hero count), Copywriting Contract (hero count format)
    - .planning/phases/04-sessions-and-buddy-pairs/04-CONTEXT.md — D-08, D-09 (header spec and hero element requirement)
  </read_first>
  <action>
SessionHeader.tsx — "use client" (contains CloseSessionDialog and LogoutButton which are both client components).
Props: `{ poolName: string; swimmerCount: number; pairCount: number; sessionId: string; poolId: string }`
Imports: CloseSessionDialog from "./CloseSessionDialog"; LogoutButton from "./LogoutButton".

Outer element: `<header className="sticky top-0 z-10 bg-white border-b border-slate-200">`.
Inner wrapper: `<div className="flex items-center justify-between px-4 py-3">`.

Left slot: `<span className="text-xl font-semibold text-slate-900">{poolName}</span>`.

Center slot: `<div className="text-center"><p className="text-4xl font-semibold text-slate-900 leading-none">{swimmerCount} swimmers · {pairCount} pairs</p></div>`. This is the hero count (D-09 — 36px text-4xl, semibold, slate-900).

Right slot: `<div className="flex items-center">`. Contains:
- CloseSessionDialog with sessionId, poolId, activePairCount={pairCount} props.
- LogoutButton with className="text-sm text-slate-600 hover:text-slate-900 ml-4".

SessionBoard.tsx — Server Component (no client state; pure composition).
Props:
```
{
  poolName: string;
  swimmerCount: number;
  pairCount: number;
  sessionId: string;
  poolId: string;
  pairs: Array<{ id: string; members: Array<{ camperId: string; firstName: string; lastName: string; bunk: string }> }>;
}
```
Imports: SessionHeader from "./SessionHeader"; AddPairForm from "./AddPairForm"; PairList from "./PairList".

Outer wrapper: `<div className="min-h-screen bg-white">`.
Renders in order:
1. `<SessionHeader poolName={poolName} swimmerCount={swimmerCount} pairCount={pairCount} sessionId={sessionId} poolId={poolId} />`
2. `<div className="p-4"><AddPairForm sessionId={sessionId} poolId={poolId} /></div>`
3. `<PairList pairs={pairs} sessionId={sessionId} poolId={poolId} />`

No additional layout logic — page.tsx controls data fetching; SessionBoard only renders.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SessionHeader.tsx: outer `<header>` has class `sticky top-0 z-10 bg-white border-b border-slate-200`
    - SessionHeader.tsx: hero count element has class `text-4xl font-semibold text-slate-900`
    - SessionHeader.tsx: hero count format is `{swimmerCount} swimmers · {pairCount} pairs` (middle dot ·, not dash)
    - SessionHeader.tsx: CloseSessionDialog receives `activePairCount={pairCount}`
    - SessionHeader.tsx: LogoutButton appears after CloseSessionDialog with `ml-4` class
    - SessionBoard.tsx: renders SessionHeader, AddPairForm, PairList in that order
    - SessionBoard.tsx: does NOT contain any useState, useEffect, or data-fetching logic (pure composition)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SessionHeader and SessionBoard compile with no TypeScript errors. The full session UI tree is composable and ready for the page.tsx in Wave 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AddPairForm → addPairAction | camper IDs are client-supplied; server validates via FK constraints |
| LogoutButton → authClient.signOut() | browser-only; no server trust boundary (auth session is invalidated server-side by Better Auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | AddPairForm camper ID submission | mitigate | addPairAction calls requireAuth and DB FK constraints reject invalid camper IDs at the storage level |
| T-04-12 | Elevation of Privilege | LogoutButton | accept | signOut invalidates the Better Auth server session; client-side redirect to /login is defense-in-depth |
| T-04-13 | Spoofing | SessionHeader hero count | accept | swimmerCount/pairCount are derived server-side from DB queries in the Server Component; client cannot spoof them |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after all five files are written
- SessionHeader hero element uses `text-4xl font-semibold`
- AddPairForm Add pair button disabled logic matches `!camper1 || !camper2`
- SessionBoard renders components in order: SessionHeader → AddPairForm → PairList
- LogoutButton calls authClient.signOut() then router.push("/login")
</verification>

<success_criteria>
Five composite components compile without TypeScript errors. SessionBoard correctly composes the full session UI from leaf components. The LogoutButton can be imported anywhere in the (protected) route group.
</success_criteria>

<output>
Create `.planning/phases/04-sessions-and-buddy-pairs/04-04-SUMMARY.md` when done.
</output>
