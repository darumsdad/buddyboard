---
phase: 04-sessions-and-buddy-pairs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
autonomous: true
requirements:
  - PAIR-04
  - SESS-01
  - SESS-03

must_haves:
  truths:
    - "The pool_session, pair, and pair_member tables exist in the Supabase database with the correct columns and constraints"
    - "Inserting two pairMember rows with the same camperId + sessionId fails at the DB level (PAIR-04 enforced)"
    - "Only one pool_session with status='active' can exist per pool at a time (D-01 partial unique index)"
    - "All cascade deletes work: deleting a pool_session removes its pairs; deleting a pair removes its pairMember rows"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "poolSession, pair, pairMember table definitions + relations"
      contains: "export const poolSession"
  key_links:
    - from: "src/db/schema.ts poolSession"
      to: "pool table"
      via: "poolId FK with onDelete cascade"
      pattern: "references.*pool\\.id"
    - from: "src/db/schema.ts pairMember"
      to: "pairMember unique constraint"
      via: "uniqueIndex on camperId + sessionId"
      pattern: "unique_camper_per_session"
---

<objective>
Add the three swim-session tables (poolSession, pair, pairMember) to the Drizzle schema and push the schema to the Supabase PostgreSQL database.

Purpose: All Wave 2+ code references these tables. No application code that queries poolSession, pair, or pairMember can compile or run until this plan completes. This is the BLOCKING prerequisite for the entire phase.

Output: Updated src/db/schema.ts with three new tables and relations; Supabase database with pool_session, pair, pair_member tables and their indexes.
</objective>

<execution_context>
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-RESEARCH.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-PATTERNS.md
</execution_context>

<context>
@C:\Users\darum\WebstormProjects\buddyboard\.planning\PROJECT.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\ROADMAP.md

<interfaces>
<!-- Existing schema exports that the new tables reference as FK targets -->
<!-- From src/db/schema.ts (read the file before editing) -->

export const user = pgTable("user", { id: text("id").primaryKey(), ... });
export const pool = pgTable("pool", { id: text("id").primaryKey(), name: text("name").notNull(), ... });
export const camper = pgTable("camper", { id: text("id").primaryKey(), firstName, lastName, code, bunk, ... });

Current imports (line 1-2 of schema.ts):
  import { relations } from "drizzle-orm";
  import { pgTable, text, timestamp, boolean, index } from "drizzle-orm/pg-core";

Phase 4 must extend these to add uniqueIndex and sql:
  import { relations, sql } from "drizzle-orm";
  import { pgTable, text, timestamp, boolean, index, uniqueIndex } from "drizzle-orm/pg-core";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add poolSession, pair, pairMember tables and relations to schema.ts</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts — read all 134 lines; understand existing import line, pool/camper/user table definitions, and relations pattern before editing
    - .planning/phases/04-sessions-and-buddy-pairs/04-PATTERNS.md — section "src/db/schema.ts" for the exact table definitions and relations to add
    - .planning/phases/04-sessions-and-buddy-pairs/04-RESEARCH.md — "Schema Additions" code example for uniqueIndex().where() syntax and the pitfall about the `session` table name collision
  </read_first>
  <action>
Modify src/db/schema.ts to add three tables and their relations. Do NOT modify any existing table definitions or relations.

Step 1 — Update the import lines at the top of the file:
- Add `sql` to the drizzle-orm import: `import { relations, sql } from "drizzle-orm";`
- Add `uniqueIndex` to the pg-core import: `import { pgTable, text, timestamp, boolean, index, uniqueIndex } from "drizzle-orm/pg-core";`

Step 2 — Append the `poolSession` table after the existing `pool` table definition. The Drizzle TypeScript name is `poolSession`; the PostgreSQL table name is `pool_session`. Columns: id (text PK), poolId (text FK → pool.id onDelete cascade), status (text notNull default "active"), openedById (text nullable FK → user.id onDelete set null), openedAt (timestamp defaultNow notNull), closedAt (timestamp nullable). Indexes: a partial uniqueIndex named "unique_active_session_per_pool" on poolId where `sql\`status = 'active'\`` (this enforces D-01 — one active session per pool at the DB level), index "pool_session_pool_id_idx" on poolId, index "pool_session_status_idx" on status.

Step 3 — Append the `pair` table. Drizzle name: `pair`; DB table: `pair`. Columns: id (text PK), sessionId (text FK → poolSession.id onDelete cascade), createdAt (timestamp defaultNow notNull). No additional indexes needed.

Step 4 — Append the `pairMember` table. Drizzle name: `pairMember`; DB table: `pair_member`. Columns: pairId (text FK → pair.id onDelete cascade), camperId (text FK → camper.id onDelete cascade), sessionId (text FK → poolSession.id onDelete cascade — denormalized to enable the unique constraint). Indexes: uniqueIndex named "unique_camper_per_session" on (camperId, sessionId) — this is the PAIR-04 enforcement at the DB level; index "pair_member_pair_id_idx" on pairId. Note: pairMember has NO separate id primary key — the combination of pairId+camperId is sufficient (or add a composite PK if Drizzle requires one; otherwise omit the explicit PK).

Step 5 — Append the three relations after the existing relation definitions:
- `poolSessionRelations`: one pool (via poolId), many pairs
- `pairRelations`: one poolSession (via sessionId), many pairMembers
- `pairMemberRelations`: one pair (via pairId), one camper (via camperId)

CRITICAL: Do NOT name any new export `session` — the auth session table already occupies that name (line 25 of schema.ts). The swim session export must be `poolSession`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - schema.ts line 1: `import { relations, sql } from "drizzle-orm"` (sql added)
    - schema.ts contains `export const poolSession = pgTable("pool_session",`
    - schema.ts contains `uniqueIndex("unique_active_session_per_pool")`
    - schema.ts contains `where(sql\`status = 'active'\`)`
    - schema.ts contains `export const pair = pgTable("pair",`
    - schema.ts contains `export const pairMember = pgTable("pair_member",`
    - schema.ts contains `uniqueIndex("unique_camper_per_session").on(table.camperId, table.sessionId)`
    - schema.ts contains `export const poolSessionRelations = relations(`
    - schema.ts contains `export const pairRelations = relations(`
    - schema.ts contains `export const pairMemberRelations = relations(`
    - `npx tsc --noEmit` exits 0 with no errors
    - The existing `session` table export (Better Auth) is unchanged
  </acceptance_criteria>
  <done>schema.ts compiles with no TypeScript errors and exports poolSession, pair, pairMember tables with their relations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run drizzle-kit push and verify tables in database</name>
  <files></files>
  <read_first>
    - src/db/schema.ts — verify Task 1 completed correctly before running push
  </read_first>
  <action>
Run `npx drizzle-kit push` from the project root. This command reads src/db/schema.ts and applies changes to the Supabase PostgreSQL database. It may prompt interactively — if it asks about creating new tables, confirm yes. If it asks about dropping or modifying the existing `session` table, reject and investigate (that would indicate a naming conflict).

After the push completes successfully (exit 0), verify in Supabase dashboard or via psql that the three tables exist with the expected structure.
  </action>
  <how-to-verify>
    1. Run `npx drizzle-kit push` — confirm it exits 0 and reports creating pool_session, pair, pair_member tables (not modifying the existing session table).
    2. Open Supabase dashboard → Table Editor — confirm pool_session, pair, pair_member tables appear.
    3. Confirm pool_session has columns: id, pool_id, status, opened_by_id, opened_at, closed_at.
    4. Confirm pair_member has a unique constraint visible in the indexes panel: unique_camper_per_session on (camper_id, session_id).
    5. Confirm pool_session has a partial unique index: unique_active_session_per_pool on pool_id WHERE status = 'active'.
  </how-to-verify>
  <resume-signal>Type "pushed" if drizzle-kit push succeeded and tables are visible in Supabase, or describe any errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.ts → drizzle-kit push | DDL changes applied to production-adjacent Supabase DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | pair_member unique constraint | mitigate | uniqueIndex("unique_camper_per_session") on (camper_id, session_id) enforces PAIR-04 at the storage layer |
| T-04-02 | Tampering | pool_session partial unique index | mitigate | uniqueIndex("unique_active_session_per_pool").where(sql`status = 'active'`) prevents two active sessions per pool at DB level |
| T-04-03 | Tampering | existing `session` table (Better Auth) | mitigate | New table named `poolSession`/`pool_session` — never `session` — prevents accidental schema collision |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; drizzle-kit already in devDependencies |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after schema.ts edits
- `npx drizzle-kit push` exits 0 and creates pool_session, pair, pair_member tables
- Supabase shows unique_camper_per_session index on pair_member
- Supabase shows unique_active_session_per_pool partial index on pool_session
- The Better Auth `session` table is unchanged
</verification>

<success_criteria>
Three tables (pool_session, pair, pair_member) exist in the Supabase database with correct columns, foreign keys, and indexes. The PAIR-04 unique constraint and the D-01 partial unique index are both active at the database level. TypeScript compilation passes.
</success_criteria>

<output>
Create `.planning/phases/04-sessions-and-buddy-pairs/04-01-SUMMARY.md` when done.
</output>
