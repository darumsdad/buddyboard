---
phase: 04-sessions-and-buddy-pairs
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - src/app/(protected)/pools/page.tsx
  - src/app/(protected)/pools/[poolId]/page.tsx
autonomous: false
requirements:
  - SESS-01
  - SESS-04

must_haves:
  truths:
    - "The /pools page shows all pools from the database as navigation links to /pools/[poolId]"
    - "The /pools page shows an empty state when no pools exist in the DB"
    - "Navigating to /pools/[poolId] auto-creates an active session if none exists for that pool (SESS-01)"
    - "Navigating to /pools/[poolId] when an active session already exists shows the JoinSessionModal before the board"
    - "The session board shows the correct swimmer count, pair count, and pair list from the DB"
    - "Closed sessions do not appear in the /pools view (SESS-04)"
    - "Unauthenticated access to /pools or /pools/[poolId] redirects to /login"
  artifacts:
    - path: "src/app/(protected)/pools/page.tsx"
      provides: "DB-driven pool selection page with Log out button"
    - path: "src/app/(protected)/pools/[poolId]/page.tsx"
      provides: "Session Server Component: get-or-create, pair fetch, count aggregation, conditional JoinSessionModal"
  key_links:
    - from: "src/app/(protected)/pools/[poolId]/page.tsx"
      to: "poolSession table"
      via: "getOrCreateActiveSession pattern from RESEARCH.md Pattern 2"
      pattern: "onConflictDoNothing"
    - from: "src/app/(protected)/pools/page.tsx"
      to: "pool table"
      via: "db.select().from(pool).orderBy(pool.name)"
      pattern: "orderBy.*pool\\.name"
    - from: "/pools/[poolId]/page.tsx"
      to: "SessionBoard"
      via: "passes session, pairs, counts as props"
      pattern: "SessionBoard.*sessionId.*pairs"
---

<objective>
Wire the two server-side pages that connect the database to the UI components built in Waves 1–4.

Purpose: This is the final integration step. After this plan, a counselor can open the app, select a pool, manage pairs, and close the session entirely over HTTP.

Output: Updated pools/page.tsx (DB-driven, logout) and new pools/[poolId]/page.tsx (get-or-create session, pair data, conditional JoinSessionModal).
</objective>

<execution_context>
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-RESEARCH.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-PATTERNS.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-UI-SPEC.md
</execution_context>

<context>
@C:\Users\darum\WebstormProjects\buddyboard\.planning\PROJECT.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\ROADMAP.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-01-SUMMARY.md
@C:\Users\darum\WebstormProjects\buddyboard\.planning\phases\04-sessions-and-buddy-pairs\04-04-SUMMARY.md

<interfaces>
<!-- Components available from plan 04-04 -->
import { SessionBoard } from "./components/SessionBoard";
import { JoinSessionModal } from "./components/JoinSessionModal";
import { LogoutButton } from "./components/LogoutButton";

<!-- SessionBoard props (from plan 04-04) -->
type SessionBoardProps = {
  poolName: string;
  swimmerCount: number;
  pairCount: number;
  sessionId: string;
  poolId: string;
  pairs: Array<{ id: string; members: Array<{ camperId: string; firstName: string; lastName: string; bunk: string }> }>;
};

<!-- Existing pools/page.tsx structure (read the file before editing) -->
// Imports: Link, headers, redirect, auth
// Auth check: const session = await auth.api.getSession({ headers: await headers() });
// Admin link: {session.user.role === "admin" && <Link href="/admin/users">Admin</Link>}
// Hardcoded: ["Main Pool", "Lap Pool", "Kiddie Pool"].map(pool => <button>)

<!-- get-or-create pattern from RESEARCH.md Pattern 2 -->
async function getOrCreateActiveSession(poolId: string, openedById: string):
  Promise<{ session: typeof poolSession.$inferSelect; wasJustCreated: boolean }>

<!-- getPairsForSession pattern from RESEARCH.md -->
Returns: Array<{ pairId: string; members: Array<{ camperId, firstName, lastName, bunk }> }>

<!-- Next.js 16: params is a Promise -->
export default async function PoolSessionPage({
  params,
}: {
  params: Promise<{ poolId: string }>;
}) {
  const { poolId } = await params;
  // ...
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pools/page.tsx — DB-driven pool list + logout</name>
  <files>src/app/(protected)/pools/page.tsx</files>
  <read_first>
    - src/app/(protected)/pools/page.tsx — read the current file in full; understand what to preserve (auth check, Admin link, Tailwind layout) and what to replace (hardcoded pool array)
    - src/app/(admin)/admin/campers/page.tsx — DB query pattern: `import { db } from "@/db"; const pools = await db.select().from(pool).orderBy(pool.name);`
    - .planning/phases/04-sessions-and-buddy-pairs/04-PATTERNS.md — section "src/app/(protected)/pools/page.tsx (component, request-response)" for change list and LogoutButton import
    - .planning/phases/04-sessions-and-buddy-pairs/04-UI-SPEC.md — Component 1 (Pool Selection Page) and Page Layout Contract (/pools ASCII diagram)
    - node_modules/next/dist/docs/ — scan for Link usage in App Router if unsure about href format
  </read_first>
  <action>
Rewrite pools/page.tsx preserving the auth check and admin link, replacing the hardcoded pool array with DB-driven data.

Changes:
1. Add imports: `db` from "@/db"; `pool` from "@/db/schema"; `orderBy, asc` from "drizzle-orm" (or just pass no orderBy and use orderBy(pool.name)); LogoutButton from "@/app/(protected)/pools/[poolId]/components/LogoutButton" (adjust path if needed — components are in the [poolId] subfolder, so the import path from pools/page.tsx is "./[poolId]/components/LogoutButton").

2. After the auth check, add: `const pools = await db.select().from(pool).orderBy(pool.name);` This fetches all pools in alphabetical order. No session status filter here — the pool list shows all configured pools; closed sessions are invisible because navigating to /pools/[poolId] always operates on the active session.

3. Replace the hardcoded `["Main Pool", "Lap Pool", "Kiddie Pool"].map(pool => <button>)` with `pools.map(p => <Link key={p.id} href={/pools/${p.id}} className="min-h-[44px] w-full bg-blue-600 hover:bg-blue-700 text-white text-base font-semibold rounded-md transition-colors duration-150 flex items-center justify-center">{p.name}</Link>)`.

4. Add empty state inside the card: when pools.length === 0, show `<p className="text-base text-slate-500 text-center">No pools configured — contact your administrator.</p>` instead of the pool list.

5. Add LogoutButton to the top bar — next to the Admin link. The top bar currently uses `absolute top-4 right-4` positioning. Render a `<div className="absolute top-4 right-4 flex items-center gap-4">` containing the Admin link (conditional on admin role) and `<LogoutButton />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - pools/page.tsx imports `db` from "@/db" and `pool` from "@/db/schema"
    - pools/page.tsx calls `await db.select().from(pool).orderBy(pool.name)`
    - pools/page.tsx renders `<Link href={/pools/${p.id}}>` for each pool (not `<button>`)
    - pools/page.tsx includes the empty state text "No pools configured — contact your administrator."
    - pools/page.tsx renders `<LogoutButton />` in the top bar
    - The Admin link is still conditional on `session.user.role === "admin"`
    - `npx tsc --noEmit` exits 0
    - No hardcoded `["Main Pool", "Lap Pool", "Kiddie Pool"]` array remains
  </acceptance_criteria>
  <done>pools/page.tsx shows DB-driven pool list as navigation links and includes a logout button. Empty state handles the no-pools case.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create pools/[poolId]/page.tsx — session Server Component + end-to-end verification</name>
  <files>src/app/(protected)/pools/[poolId]/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/campers/page.tsx — full file; the closest analog: async Server Component with auth check, DB query, awaited searchParams pattern; use this for import structure and query pattern
    - .planning/phases/04-sessions-and-buddy-pairs/04-PATTERNS.md — section "src/app/(protected)/pools/[poolId]/page.tsx" for the full get-or-create pattern, getPairsForSession helper, and async params usage
    - .planning/phases/04-sessions-and-buddy-pairs/04-RESEARCH.md — Pattern 2 (getOrCreateActiveSession with onConflictDoNothing), "Fetching Pairs for Session Page", "Counting Swimmers and Pairs", Pitfall 5 (params is a Promise)
    - node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md — read before writing; AGENTS.md requires reading Next.js docs before writing Next.js-specific code
  </read_first>
  <action>
Create `src/app/(protected)/pools/[poolId]/page.tsx` as an async Server Component.

AGENTS.md requires reading the Next.js docs listed above before writing this file.

Component signature (from RESEARCH.md Pitfall 5 — params is a Promise in Next.js 16):
```
export default async function PoolSessionPage({
  params,
}: {
  params: Promise<{ poolId: string }>;
}) {
  const { poolId } = await params; // must await
```

Steps inside the component:

Step 1 — Auth check: `const authSession = await auth.api.getSession({ headers: await headers() }); if (!authSession) redirect("/login");`

Step 2 — Pool lookup: `const poolRecord = await db.select().from(pool).where(eq(pool.id, poolId)).limit(1); if (!poolRecord[0]) redirect("/pools");` (handles invalid poolId gracefully)

Step 3 — Get-or-create active session: implement the `getOrCreateActiveSession(poolId, authSession.user.id)` helper inline or as a private async function. Copy the pattern verbatim from RESEARCH.md Pattern 2. It returns `{ session, wasJustCreated }`. `wasJustCreated === false` means a session pre-existed this page load (show JoinSessionModal). `wasJustCreated === true` means this request created the session (show board directly).

Step 4 — Fetch pairs: implement `getPairsForSession(sessionId)` as a private helper. Query pairMember joined with camper, where pairMember.sessionId = session.id. Group results by pairId in application code (simple Map reduce). Returns `Array<{ id: string; members: Array<{ camperId, firstName, lastName, bunk }> }>`.

Step 5 — Count swimmers and pairs in parallel:
```
const [swimmerCount, pairCount] = await Promise.all([
  db.select({ count: sql<number>`count(*)` }).from(pairMember).where(eq(pairMember.sessionId, session.id)).then(r => Number(r[0]?.count ?? 0)),
  db.select({ count: sql<number>`count(*)` }).from(pair).where(eq(pair.sessionId, session.id)).then(r => Number(r[0]?.count ?? 0)),
]);
```

Step 6 — Render:
- Import JoinSessionModal from "./components/JoinSessionModal"
- Import SessionBoard from "./components/SessionBoard"
- Return JSX: if `!wasJustCreated`, render `<JoinSessionModal poolName={poolRecord[0].name} />` before (or overlaid over) the board. The modal uses `fixed inset-0` so it overlays everything — render it as a sibling of SessionBoard: `<><JoinSessionModal poolName={poolRecord[0].name} /><SessionBoard ... /></>`.
- Always render SessionBoard with: poolName, swimmerCount, pairCount, sessionId=session.id, poolId, pairs.

Imports needed: headers from "next/headers"; redirect from "next/navigation"; auth from "@/lib/auth"; db from "@/db"; poolSession, pair, pairMember, pool, camper from "@/db/schema"; eq, and, sql from "drizzle-orm".
  </action>
  <how-to-verify>
    Run `npm run dev` and perform the following end-to-end checks:
    1. Navigate to /pools — confirm the list shows all DB pools as clickable links (not hardcoded buttons).
    2. Click a pool link — confirm you land on /pools/[poolId] and see the empty board ("No pairs checked in yet") with the sticky header showing the pool name and "0 swimmers · 0 pairs".
    3. Refresh /pools/[poolId] — confirm the JoinSessionModal appears ("session in progress") since the session now pre-exists. Click "Join session" to dismiss it.
    4. Use the Add pair form: type a camper code in Camper 1 and confirm it resolves to a chip. Type a name in Camper 2 and confirm typeahead dropdown appears. Select a camper and click "Add pair".
    5. Confirm the pair appears in the pair list with the correct names and +1 / Remove buttons.
    6. Click Remove on the pair — confirm the row disappears after server confirmation.
    7. Add two pairs, then click "Close session" — confirm the confirmation dialog appears showing the pair count. Click "Keep session open" to cancel. Click "Close session" again, then confirm — confirm redirect to /pools.
    8. Click the same pool again — confirm a new session starts (empty board, no JoinSessionModal since the session was just created).
    9. Click "Log out" — confirm redirect to /login.
    10. `npx vitest run` — confirm the full test suite is green.
  </how-to-verify>
  <resume-signal>Type "verified" if all 10 checks pass, or describe which steps failed and what the actual behavior was.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /pools/[poolId] GET request | poolId URL parameter is user-controlled; must be validated against DB |
| JoinSessionModal "Go back" | Client-side navigation; no server trust concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | poolId URL parameter | mitigate | db.select().from(pool).where(eq(pool.id, poolId)) validates the ID; invalid poolId redirects to /pools |
| T-04-15 | Elevation of Privilege | /pools/[poolId] Server Component | mitigate | auth.api.getSession check before any DB access; unauthenticated requests redirect to /login |
| T-04-16 | Tampering | get-or-create race condition | mitigate | onConflictDoNothing() + re-fetch pattern handles concurrent session creation; DB partial unique index is the final arbiter |
| T-04-17 | Information Disclosure | pools/page.tsx pool list | accept | All authenticated counselors can see all pool names — this is the intended shared workspace model |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after both files are written
- `npx vitest run` exits 0 with all tests green
- /pools shows DB pools as Link elements (not hardcoded buttons)
- /pools/[poolId] shows JoinSessionModal when session pre-exists (wasJustCreated=false)
- /pools/[poolId] shows empty board when session is just created (wasJustCreated=true)
- End-to-end: add pair → pair appears; remove pair → pair disappears; close session → redirect to /pools
</verification>

<success_criteria>
Both pages render correctly. A counselor can complete a full session lifecycle: select pool → add pairs → remove pairs → close session. All 8 requirement IDs (SESS-01 through PAIR-04) are satisfied end-to-end. The full vitest suite is green.
</success_criteria>

<output>
Create `.planning/phases/04-sessions-and-buddy-pairs/04-05-SUMMARY.md` when done.
</output>
