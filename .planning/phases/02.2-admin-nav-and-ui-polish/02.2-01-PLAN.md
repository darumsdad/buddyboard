---
phase: 02.2-admin-nav-and-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(admin)/admin/users/page.tsx
  - src/app/(protected)/pools/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "Admin who logs in is immediately pushed to /admin/users without an intermediate redirect"
    - "Counselor (non-admin) who logs in is pushed to /pools"
    - "Admin users page has a 'View Buddy Board' link in the header row, right side, that navigates to /pools"
    - "Pools page renders an 'Admin' link in the top-right corner only when session.user.role === 'admin'"
    - "Pools page 'Admin' link navigates to /admin/users"
    - "Counselor viewing the pools page sees no 'Admin' link"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Role-based redirect after sign-in"
      contains: "admin/users"
    - path: "src/app/(admin)/admin/users/page.tsx"
      provides: "View Buddy Board nav link in header"
      contains: "View Buddy Board"
    - path: "src/app/(protected)/pools/page.tsx"
      provides: "Conditional Admin nav link"
      contains: "admin/users"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "authClient.signIn.username"
      via: "destructured data.user.role from response"
      pattern: "data\\.user\\.role"
    - from: "src/app/(protected)/pools/page.tsx"
      to: "/admin/users"
      via: "session.user.role === 'admin' conditional"
      pattern: "session\\.user\\.role"
---

<objective>
Wire three small navigation changes that make the app coherent: role-aware post-login redirect, a "View Buddy Board" link on the admin screen, and a conditional "Admin" link on the pools screen.

Purpose: After these changes, every user lands on the correct screen after login and has a one-click path to the other screen (admin only sees admin link on pools; all admins see buddy board link on admin screen).
Output: Three modified files. No new files, no schema changes, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.2-admin-nav-and-ui-polish/02.2-CONTEXT.md
@.planning/phases/02-admin-user-management/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-based redirect in login page (D-01, D-02)</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <read_first>
    - src/app/(auth)/login/page.tsx — read the full current file before touching anything; locate the handleSubmit function and the line `router.push("/pools")` (currently line 28)
    - src/lib/auth-client.ts — confirm authClient.signIn.username returns { data, error }
  </read_first>
  <action>
    In src/app/(auth)/login/page.tsx, inside handleSubmit, change the destructuring at the authClient.signIn.username call from `const { error: signInError }` to `const { data, error: signInError }`. Then replace the single `router.push("/pools")` line in the else branch with a role check: if `data?.user?.role === "admin"` push to `/admin/users`, else push to `/pools`. No other changes to the file. No new imports needed — router and authClient are already imported.

    Per D-01: role is read from `data.user.role` on the signIn response.
    Per D-02: no new files or root-page router are introduced.
  </action>
  <verify>
    <automated>grep -n "admin/users" src/app/(auth)/login/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data?.user?.role" src/app/(auth)/login/page.tsx` returns a match on the role-check line
    - `grep -n "admin/users" src/app/(auth)/login/page.tsx` returns a match on the push line
    - `grep -c "router.push" src/app/(auth)/login/page.tsx` returns 2 (one for /admin/users, one for /pools)
    - The file still has no new import statements beyond what existed before (no new imports added)
  </acceptance_criteria>
  <done>handleSubmit reads data.user.role from signIn response and pushes admins to /admin/users, all other roles to /pools</done>
</task>

<task type="auto">
  <name>Task 2: Nav links on admin page and pools page (D-03, D-04, D-05)</name>
  <files>src/app/(admin)/admin/users/page.tsx, src/app/(protected)/pools/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/users/page.tsx — read full file; note that the header is a `div className="flex justify-between items-center"` containing the h1 on the left and `<CreateUserModal />` on the right (currently line 39-44)
    - src/app/(protected)/pools/page.tsx — read full file; note the outer div is `className="bg-white min-h-screen flex items-center justify-center"` and session is already fetched on line 6
  </read_first>
  <action>
    **Admin users page (src/app/(admin)/admin/users/page.tsx):**

    Add `import Link from "next/link"` at the top of the imports. Inside the existing `div className="flex justify-between items-center"` header row, replace the standalone `<CreateUserModal />` on the right with a `div className="flex items-center gap-4"` containing: first a `Link href="/pools" className="text-sm text-slate-600 hover:text-slate-900"` with inner text "View Buddy Board", then `<CreateUserModal />`. This keeps the h1 left-anchored and puts both the link and the create button right-anchored, separated by 16px (gap-4).

    Per D-03: the link text is "View Buddy Board", href="/pools", styled text-sm text-slate-600 hover:text-slate-900.
    Per D-05: subtle link style, does not compete with the blue primary button.

    **Pools page (src/app/(protected)/pools/page.tsx):**

    Add `import Link from "next/link"` at the top of the imports. Change the outer div's className from `"bg-white min-h-screen flex items-center justify-center"` to `"bg-white min-h-screen flex items-center justify-center relative"`. Immediately inside that outer div, before the centered pool card div, add: `{session.user.role === "admin" && (<Link href="/admin/users" className="absolute top-4 right-4 text-sm text-slate-600 hover:text-slate-900">Admin</Link>)}`.

    Per D-04: the link is only rendered when session.user.role === "admin".
    Per D-05: same subtle link style.

    Note: session is already fetched and non-null at the point of rendering (the redirect on line 7 handles the null case). Access session.user.role directly without optional chaining.
  </action>
  <verify>
    <automated>grep -n "View Buddy Board" src/app/(admin)/admin/users/page.tsx && grep -n "admin/users" src/app/(protected)/pools/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "View Buddy Board" src/app/(admin)/admin/users/page.tsx` returns exactly 1 match
    - `grep -n "href=\"/pools\"" src/app/(admin)/admin/users/page.tsx` returns a match on the Link element
    - `grep -n "next/link" src/app/(admin)/admin/users/page.tsx` returns a match (import added)
    - `grep -n "admin/users" src/app/(protected)/pools/page.tsx` returns a match on the Link href
    - `grep -n "session.user.role" src/app/(protected)/pools/page.tsx` returns a match on the conditional
    - `grep -n "next/link" src/app/(protected)/pools/page.tsx` returns a match (import added)
    - `grep -n "relative" src/app/(protected)/pools/page.tsx` returns a match on the outer div classname
    - `grep -n "absolute top-4 right-4" src/app/(protected)/pools/page.tsx` returns a match on the link positioning
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>"View Buddy Board" link appears in admin page header right-side; "Admin" link appears on pools page top-right only for admin-role sessions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signIn response → router.push | Client receives user.role from auth server in signIn response; role drives UI redirect only |
| pools page → Admin link render | session fetched server-side; role check is server-evaluated before HTML is sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-01-01 | Elevation of Privilege | login/page.tsx role-based redirect | accept | Redirect is UI-only; actual admin route protection is enforced server-side in (admin)/layout.tsx role check — client-side role manipulation only changes which URL the client requests, not what it can access |
| T-02.2-01-02 | Information Disclosure | pools/page.tsx Admin link | accept | Link visibility controlled by server-rendered session role check; even if a non-admin crafts a request to /admin/users, the layout.tsx gate denies access |
</threat_model>

<verification>
After both tasks complete:
- Login page: `grep -n "data?.user?.role" src/app/(auth)/login/page.tsx` — confirms role check present
- Admin page: `grep -n "View Buddy Board" src/app/(admin)/admin/users/page.tsx` — confirms nav link
- Pools page: `grep -n "session.user.role === .admin." src/app/(protected)/pools/page.tsx` — confirms conditional link
- Type check: `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Admin credentials → sign in → lands on /admin/users (not /pools)
- Non-admin credentials → sign in → lands on /pools
- /admin/users page header row shows "View Buddy Board" link on the right, next to "Create user" button
- /pools page shows "Admin" link in top-right corner when accessed by admin; no link when accessed by counselor
- All three link styles are text-sm text-slate-600 hover:text-slate-900
</success_criteria>

<output>
Create `.planning/phases/02.2-admin-nav-and-ui-polish/02.2-01-SUMMARY.md` when done
</output>
