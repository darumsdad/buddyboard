---
phase: 02.2-admin-nav-and-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/(admin)/admin/users/components/ResetPasswordForm.tsx
  - src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx
autonomous: false
requirements:
  - D-06
  - D-07
  - D-08
  - D-09

user_setup: []

must_haves:
  truths:
    - "lucide-react is installed and importable from node_modules"
    - "Reset password trigger is a round icon button (40x40px, rounded-full) containing a Key icon at size 18"
    - "Reset password trigger button has title='Reset password' for tooltip discoverability"
    - "Delete user trigger is a round icon button (40x40px, rounded-full) containing a Trash2 icon at size 18"
    - "Delete user trigger button has title='Delete user' for tooltip discoverability"
    - "Reset button uses text-slate-600 hover:bg-slate-100 color treatment"
    - "Delete button uses text-red-600 hover:bg-red-50 color treatment"
  artifacts:
    - path: "src/app/(admin)/admin/users/components/ResetPasswordForm.tsx"
      provides: "Round icon button trigger with Key icon"
      contains: "lucide-react"
    - path: "src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx"
      provides: "Round icon button trigger with Trash2 icon"
      contains: "lucide-react"
  key_links:
    - from: "ResetPasswordForm.tsx trigger button"
      to: "Key icon"
      via: "import { Key } from 'lucide-react'"
      pattern: "Key.*size.*18"
    - from: "DeleteConfirmDialog.tsx trigger button"
      to: "Trash2 icon"
      via: "import { Trash2 } from 'lucide-react'"
      pattern: "Trash2.*size.*18"
---

<objective>
Install lucide-react and replace the two text-link-styled trigger buttons in the UserTable actions column with accessible round icon buttons.

Purpose: The actions column currently has text links ("Reset password", "Delete user") that look out of place. Round icon buttons with native title tooltips are more compact, visually consistent with icon-based action patterns, and accessible via keyboard.
Output: lucide-react in package.json, ResetPasswordForm.tsx and DeleteConfirmDialog.tsx updated to round icon button triggers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.2-admin-nav-and-ui-polish/02.2-CONTEXT.md
@.planning/phases/02-admin-user-management/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify lucide-react package legitimacy (slopcheck gate, D-06)</name>
  <what-built>No code has been written yet. This checkpoint gates the npm install of lucide-react. No RESEARCH.md Package Legitimacy Audit exists for this phase — the package must be manually verified before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lucide-react in your browser
    2. Confirm: package name is exactly "lucide-react" (not "lucide_react", "lucidereact", or any variation)
    3. Confirm: the package is published by the "lucide" organization (check the "Publisher" field)
    4. Confirm: weekly downloads are in the millions (expected: 15M+ as of 2025)
    5. Confirm: the package homepage links to lucide.dev
    6. Confirm: latest version is in the 0.x.x range (it follows semver but has not hit 1.0; versions like 0.460.0 are normal)
    If all five checks pass, type "approved". If anything looks suspicious, type "blocked" and describe what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or "blocked: [reason]" to halt</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install lucide-react and update icon button triggers (D-06, D-07, D-08, D-09)</name>
  <files>src/app/(admin)/admin/users/components/ResetPasswordForm.tsx, src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx</files>
  <read_first>
    - src/app/(admin)/admin/users/components/ResetPasswordForm.tsx — read full file; locate the trigger button (currently the first button element, around line 37-42, with class "min-h-[44px] text-base text-slate-600 underline-offset-2 hover:underline" and text "Reset password")
    - src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx — read full file; locate the trigger button (currently around line 30-35, with class "min-h-[44px] text-base text-red-600 underline-offset-2 hover:underline" and text "Delete user")
  </read_first>
  <action>
    **Step 1 — Install lucide-react:**
    Run `npm install lucide-react`. Confirm package.json now lists lucide-react as a dependency.

    **Step 2 — ResetPasswordForm.tsx (per D-06, D-07, D-08, D-09):**
    At the top of the file, add the import: `import { Key } from "lucide-react"` (add it after the existing imports).

    Find the trigger button (the first `<button>` element in the return JSX, the one that calls `setOpen(!open)`). Replace it entirely with:
    - Keep `onClick={() => setOpen(!open)}`
    - Change className to: `"w-10 h-10 rounded-full flex items-center justify-center text-slate-600 hover:bg-slate-100 transition-colors"`
    - Add attribute: `title="Reset password"`
    - Replace the text content "Reset password" with: `<Key size={18} />`

    Remove `min-h-[44px]` from this button — per D-09 the 40x40px button meets touch targets via surrounding table cell padding (px-4 py-3). The `min-h-[44px]` class stays on all other buttons in the file (Discard, Save password) — do not touch those.

    **Step 3 — DeleteConfirmDialog.tsx (per D-06, D-07, D-08, D-09):**
    At the top of the file, add the import: `import { Trash2 } from "lucide-react"` (add it after the existing imports).

    Find the trigger button (the first `<button>` element in the return JSX, inside the `<>` fragment, the one that calls `setOpen(true)`). Replace it entirely with:
    - Keep `onClick={() => setOpen(true)}`
    - Change className to: `"w-10 h-10 rounded-full flex items-center justify-center text-red-600 hover:bg-red-50 transition-colors"`
    - Add attribute: `title="Delete user"`
    - Replace the text content "Delete user" with: `<Trash2 size={18} />`

    Remove `min-h-[44px]` from this button only — the confirmation dialog's "Keep user" and "Remove user" buttons retain `min-h-[44px]` — do not touch those.

    **No other changes** — do not modify the inline form, the confirmation dialog modal, or any other component in these files.
  </action>
  <verify>
    <automated>grep -n "lucide-react" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx && grep -n "title=" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lucide-react" package.json` returns 1 (dependency entry present)
    - `grep -n "Key" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx` returns matches for both the import and the JSX usage
    - `grep -n "title=\"Reset password\"" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx` returns exactly 1 match
    - `grep -n "rounded-full" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx` returns a match on the trigger button's className
    - `grep -n "hover:bg-slate-100" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx` returns a match
    - `grep -n "underline" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx` returns 0 matches (old trigger style fully removed)
    - `grep -n "Trash2" src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` returns matches for both the import and the JSX usage
    - `grep -n "title=\"Delete user\"" src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` returns exactly 1 match
    - `grep -n "rounded-full" src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` returns a match on the trigger button's className
    - `grep -n "hover:bg-red-50" src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` returns a match
    - `grep -n "underline" src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` returns 0 matches (old trigger style fully removed)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>lucide-react installed; both component trigger buttons are 40x40px round icon buttons with Key/Trash2 icons and title attributes; text link styles removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party package install; supply chain attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-02-SC | Tampering | npm install lucide-react | mitigate | Blocking human checkpoint (Task 1) requires manual verification on npmjs.com before install proceeds — check publisher, download count, package name exact match |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "lucide-react" package.json` — confirms install
- `grep -n "rounded-full" src/app/(admin)/admin/users/components/ResetPasswordForm.tsx src/app/(admin)/admin/users/components/DeleteConfirmDialog.tsx` — confirms both triggers updated
- `grep -rn "underline-offset-2 hover:underline" src/app/(admin)/admin/users/components/` — returns 0 matches (no remnants of old text-link style)
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- lucide-react appears in package.json dependencies
- /admin/users UserTable actions column shows a round key icon button and a round trash icon button per row
- Hovering over the key button shows the native tooltip "Reset password"
- Hovering over the trash button shows the native tooltip "Delete user"
- Key button hover state: bg-slate-100 background; Trash button hover state: bg-red-50 background
- Clicking key button still opens the inline reset form (behavior unchanged)
- Clicking trash button still opens the delete confirmation dialog (behavior unchanged)
</success_criteria>

<output>
Create `.planning/phases/02.2-admin-nav-and-ui-polish/02.2-02-SUMMARY.md` when done
</output>
