---
phase: 02.2-admin-nav-and-ui-polish
plan: "01"
subsystem: auth-ui
tags: [navigation, role-based-redirect, next-link]
dependency_graph:
  requires: []
  provides: [role-aware-post-login-redirect, admin-nav-link, pools-admin-link]
  affects: [login, admin-users-page, pools-page]
tech_stack:
  added: []
  patterns: [role-check-on-signIn-response, conditional-server-rendered-link]
key_files:
  modified:
    - src/app/(auth)/login/page.tsx
    - src/app/(admin)/admin/users/page.tsx
    - src/app/(protected)/pools/page.tsx
decisions:
  - "D-01: role read from data.user.role on signIn response, no extra API call"
  - "D-02: no new files or root redirect — minimal change to login page only"
  - "D-03: View Buddy Board link text-sm text-slate-600 hover:text-slate-900 inline in header"
  - "D-04: Admin link conditionally rendered via session.user.role === 'admin'"
  - "D-05: subtle link style — does not compete with blue primary buttons"
metrics:
  duration_seconds: ~240
  completed_date: "2026-06-28"
  tasks_completed: 2
  files_modified: 3
---

# Phase 2.2 Plan 01: Role-Based Redirect and Nav Links Summary

Role-aware post-login redirect (admin to /admin/users, others to /pools) and bidirectional nav links between the admin and pools screens.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Role-based redirect in login page | 9b5290b | src/app/(auth)/login/page.tsx |
| 2 | Nav links on admin page and pools page | d382435 | src/app/(admin)/admin/users/page.tsx, src/app/(protected)/pools/page.tsx |

## What Was Built

**Task 1 — Login redirect:**
- Changed `const { error: signInError }` to `const { data, error: signInError }` on the `authClient.signIn.username()` call
- Replaced static `router.push("/pools")` with a role check: admins go to `/admin/users`, all others go to `/pools`
- No new imports needed; no new files created

**Task 2 — Nav links:**
- Admin users page: added `import Link from "next/link"`; wrapped `<CreateUserModal />` in a flex div alongside a "View Buddy Board" `<Link href="/pools">` using text-sm text-slate-600 hover:text-slate-900
- Pools page: added `import Link from "next/link"`; added `relative` to the outer div className; inserted `{session.user.role === "admin" && (<Link href="/admin/users" className="absolute top-4 right-4 text-sm text-slate-600 hover:text-slate-900">Admin</Link>)}`

## Verification Results

All acceptance criteria passed:
- `grep "data?.user?.role" login/page.tsx` — match on line 28
- `grep "admin/users" login/page.tsx` — match on line 29
- `grep -c "router.push" login/page.tsx` — returns 2
- No new imports added to login page
- `grep "View Buddy Board" admin/users/page.tsx` — exactly 1 match
- `grep 'href="/pools"' admin/users/page.tsx` — match
- `grep "next/link" admin/users/page.tsx` — match
- `grep "admin/users" pools/page.tsx` — match
- `grep "session.user.role" pools/page.tsx` — match
- `grep "next/link" pools/page.tsx` — match
- `grep "relative" pools/page.tsx` — match
- `grep "absolute top-4 right-4" pools/page.tsx` — match
- `npx tsc --noEmit` — exit 0

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all nav links reference real routes with server-enforced access control.

## Threat Flags

No new threat surface introduced beyond what was documented in the plan's threat model. Redirect is UI-only; actual admin route protection remains enforced in (admin)/layout.tsx.

## Self-Check: PASSED

- src/app/(auth)/login/page.tsx — modified, commit 9b5290b confirmed
- src/app/(admin)/admin/users/page.tsx — modified, commit d382435 confirmed
- src/app/(protected)/pools/page.tsx — modified, commit d382435 confirmed
