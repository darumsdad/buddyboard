import { describe, it } from "vitest";

// Wave 0 stub — component ConnectionBanner.tsx is created in Plan 05-02.
// These stubs keep npm test green while enabling gate-grep by requirement ID.

describe("BOARD-04", () => {
  it.todo("renders nothing when status is connected");
  it.todo("shows Reconnecting message for reconnecting status");
  it.todo(
    "shows Disconnected message and Refresh button for disconnected status",
  );
});
